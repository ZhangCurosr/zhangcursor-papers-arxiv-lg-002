# Thompson Sampling for Non-Monotone Convex Ridge Bandits: Monotonicity Is Not Needed for Polynomial Regret

Xuan Li

University of New South Wales, Sydney, Australia · winny.li@unsw.edu.au ORCID: 0009-0002-0213-6991

September 11, 2026

## Abstract

Bakhtiari, Lattimore and Szepesv´ari (COLT 2025) proved that Thompson sampling (TS) has Bayesian regret ${ \tilde { O } } ( d ^ { 5 / 2 } { \sqrt { n } } )$ for bandit convex optimisation with convex monotone ridge losses $f ( x ) = \ell ( \left. x , \theta \right. )$ , and asked whether monotonicity of the link is necessary. We give a qualitative negative answer. For every prior on [0, 1]-valued, 1-Lipschitz convex ridge losses with an arbitrary convex, possibly non-monotone, link, and for any fixed measurable selection of minimisers, exactposterior TS has Bayesian regret $O \big ( ( d + 1 ) ^ { 4 } \sqrt { d n } \log ( e + n d \operatorname* { m a x } \{ 1 , \mathrm { d i a m } K \} ) \big ) = \tilde { O } ( d ^ { 9 / 2 } \sqrt { n } )$ The monotone proof relies on a single-removal John-ellipsoid dichotomy; we show by an explicit twelve-point configuration that this dichotomy fails for non-monotone links, and replace it by an $O ( d ^ { 2 } )$ cardinality bound for “uninformative” configurations. The bound uses a Boolean rounding argument: a 0-1 matrix within 1/(4r) in max-norm of a rank-r matrix has rank at most $2 r - 1$ We construct d(d + 1) uninformative losses, showing that the cardinality bound is tight up to constants in the large-diameter-to-gap regime, and give a self-contained information-ratio-toregret transfer that is uniform over fixed measurable selections. Whether the $d ^ { 5 / 2 }$ dependence of the monotone case can be retained remains open.

## 1 Introduction

Bayesian bandit convex optimisation is the following game. A convex body $K \subset \mathbb { R } ^ { d }$ and a class F of convex functions $K  [ 0 , 1 ]$ are known, together with a prior ξ on F. The environment samples $f \sim \xi$ once. In each round $t = 1 , \ldots , n$ the learner plays $X _ { t } \in K$ and observes $Y _ { t } \in \{ 0 , 1 \}$ with $\mathbb { E } [ Y _ { t } \mid X _ { 1 } , Y _ { 1 } , \ldots , X _ { t } , f ] = f ( X _ { t } )$ . The Bayesian regret of a learner A is

$$
\mathrm { B R e g } _ { n } ( A , \xi ) = \mathbb { E } { \Big [ } \operatorname* { s u p } _ { x \in K } \sum _ { t = 1 } ^ { n } { \big ( } f ( X _ { t } ) - f ( x ) { \big ) } { \Big ] } .
$$

Thompson sampling (TS) samples $f _ { t }$ from the posterior in every round and plays a minimiser $X _ { t } = x _ { f _ { t } }$ of the sampled function. Bakhtiari, Lattimore and Szepesv´ari [1] analysed TS in this setting via the information ratio. Among their results, they showed that if $\xi$ is supported on monotone convex ridge functions, i.e. $f ( x ) = \ell ( \left. x , \theta \right. )$ with $\ell : \mathbb { R } \to \mathbb { R }$ convex and non-decreasing, then $\mathrm { B R e g } _ { n } ( \mathrm { T S } , \xi ) = O ( d ^ { 2 . 5 } \sqrt { n } \log ^ { 2 } ( n d \mathrm { d i a m } K ) )$ ; and they showed that TS can fail catastrophically on general convex losses. In their discussion they write:

The monotone ridge class is a Bayesian version of the generalised linear bandit with an unknown convex, increasing link. Dropping monotonicity allows links such as $\ell ( s ) = | s - s _ { 0 } |$ whose minimising set on K is, when $d \geq 2$ and s<sub>0</sub> lies in the interior of the projection interval $\{ \langle x , \theta \rangle : x \in K \}$ , a whole hyperplane section; minimisers of convex ridge losses can thus be non-unique, so the tie-breaking rule of TS matters, and the geometry that drives the monotone analysis (an ordering of minimisers along the ridge direction) is lost.

Our goal is statistical rather than computational: we ask whether the canonical exact-posterior TS rule itself is safe on this structured class. The existence of low-regret algorithms for convex ridge links does not settle this question. Lattimore [3] obtains $O ( d { \sqrt { n } } \log ( n D ) )$ minimax regret for an adversarial ridge model with a diferent algorithm, but the question here is algorithm-specific: [1] show that standard TS can fail catastrophically on general high-dimensional convex losses, so structure is genuinely needed to certify TS itself, and the monotone ridge class was the one for which they could do so. Our result identifies convex ridge structure as suficient even when the link is non-monotone: a qualitative robustness statement about the canonical Bayesian sampling rule, not a minimax-optimality claim.

Contributions. Let $\mathcal { F } _ { \mathrm { b l r } }$ be the class of convex ridge functions $K  [ 0 , 1 ]$ that are 1-Lipschitz, with an arbitrary convex link (Section 2).

1. Regret bound without monotonicity (Theorem 3.1). For every prior on $\mathcal { F } _ { \mathrm { b l r } }$ and every fixed measurable selection of minimisers, TS satisfies $\mathrm { B R e g } _ { n } ( \mathrm { T S } , \xi ) = \tilde { O } ( d ^ { 9 / 2 } \sqrt { n } )$ ; explicitly, $\mathrm { B R e g } _ { n } ( \mathrm { T S } , \xi ) \leq 7 + 7 3 , 7 2 8 { \sqrt { 3 } } ( d + 1 ) ^ { 4 } d ^ { 1 / 2 } { \sqrt { n } } \log ( e + n d \operatorname* { m a x } \{ 1 , \mathrm { d i a m } K \} )$ . This resolves the qualitative question of whether monotonicity is necessary for polynomial-in-d Bayesian regret of exact-posterior TS; the quantitative question of matching the $d ^ { 5 / 2 }$ dependence of the monotone case remains open, and is discussed in Section 9.

2. A cardinality bound for uninformative configurations (Theorem 3.3). The informationratio machinery of [1] reduces to showing that a finite set of loss functions whose minimisers reveal nothing about one another is small. In the monotone case this is proved by a Johnellipsoid dichotomy. We show that this single-removal dichotomy has no non-monotone analogue (Proposition 3.6) and prove directly that every such configuration has at most $3 ( d { + } 1 ) ^ { 2 } { - } ( d { + } 1 ) { - } 1$ elements. The key tool is an elementary rounding lemma (Lemma 5.3): if a 0-1 matrix is entrywise within $1 / ( 4 r )$ of a matrix of rank r then its rank is at most $2 r - 1$ . The bound is tight up to a constant: Proposition 3.5 exhibits uninformative configurations of size $d ( d + 1 )$ .

3. Transfer from information ratio to regret under any fixed measurable selection rule (Theorem 7.5). The covering argument of [1] uses that ties among minimisers are broken consistently. Since minimisers of convex ridge losses can be non-unique, we give a self-contained transfer theorem for exact TS, valid for every fixed measurable selection rule, using a cover by infimal-convolution approximations and an information-ratio bound that is uniform over selection rules. The rule is arbitrary but fixed in advance; history-dependent tie-breaking is not covered.

Related work. Ridge (single-index) bandits have been studied under diferent learning criteria and for diferent algorithms. Lattimore [3] proves $O ( d { \sqrt { n } } \log ( n D ) )$ minimax regret for an adversarial ridge model with a hidden fixed direction and convex links that may vary over time, by informationtheoretic arguments and minimax duality; as noted above, this establishes learnability of the ridge structure without bearing on standard TS under an arbitrary prior. Bakhtiari et al. [1] analyse exact TS directly and prove the monotone ridge bound; the present paper removes monotonicity within the ridge structure. Rajaraman, Han, Jiao and Ramchandran [10], Rajaraman and Han [11] and Kang et al. [13] study frequentist nonlinear ridge, single-index and contextual single-index bandits with algorithms other than TS, and Rajaraman and Han [12] give minimax lower bounds for general stochastic bandit convex optimisation, which quantify the dificulty of general convex losses but are not specific to non-monotone ridge losses or to TS. None of these results provides a Bayesian regret guarantee for exact-posterior TS over arbitrary convex non-monotone ridge links. We do not address the second question of the same paragraph of [1] (the information ratio with a known link); see also the MSc thesis of Bakhtiari [2]. Information-ratio analyses of TS go back to [7]; the convex-bandit machinery we build on is from [5, 6, 4, 1].

## 2 Setting and notation

Throughout, $K \subset \mathbb { R } ^ { d }$ is a convex body (compact, convex, non-empty interior) with $0 \in K$ , and $D = \dim ( K )$ . A function $f : K \to \mathbb { R }$ is a convex ridge function if $f ( x ) = \ell ( \left. x , \theta \right. )$ for some convex $\ell : \mathbb { R } \to \mathbb { R }$ and $\theta \in \mathbb { R } ^ { d } ~ ( \theta = 0$ is allowed). We write

$$
\begin{array} { r l r l } & { \mathcal { F } _ { \mathrm { b l } } = \big \{ f : K \to [ 0 , 1 ] \mathrm { ~ c o n v e x , ~ L i p } ( f ) \leq 1 \big \} , } & & { \mathcal { F } _ { \mathrm { b l r } } = \big \{ f \in \mathcal { F } _ { \mathrm { b l } } : \ f \mathrm { ~ i s ~ a ~ c o n v e x ~ r i d g e ~ f u n c t i o n } \big \} , } \end{array}
$$

and ${ \mathcal { F } } _ { \mathrm { b l r m } } \subset { \mathcal { F } } _ { \mathrm { b l r } }$ for the subclass with non-decreasing link. The classes $\mathcal { F } _ { \mathrm { b l } }$ and $\mathcal { F } _ { \mathrm { b l r m } }$ are those of $[ 1 ] ; { \mathcal { F } } _ { \mathrm { b l r } }$ is not treated there.

Measurability and tie-breaking (standing convention). We equip $\mathcal { F } _ { \mathrm { b l r } }$ with a σ-algebra A for which $f \mapsto f ( x )$ is measurable for every $x \in K$ and a measurable selection $f \mapsto x _ { f } \in \arg \operatorname* { m i n } _ { K } f$ has been fixed once and for all (such selections exist; see [1, Appendix B]). Pointwise measurability makes the identity $( \mathcal { F } _ { \mathrm { b l r } } , \mathcal { A } ) \to ( C ( K ) , B )$ measurable, where B is the Borel σ-algebra of the uniform norm; $\mathcal { A }$ may be strictly finer than B. All approximating functions constructed in Section 7 are measurable as $( C ( K ) , B )$ -valued maps, and there the information-ratio hypothesis is only applied to finitely-valued random elements of $\mathcal { F } _ { \mathrm { b l r } }$ , which are A-measurable automatically; this is what allows arbitrary priors and arbitrary fixed selections. All statements hold for every such selection rule, but the rule is fixed in advance: it may not depend on the history of the game. We write $f ^ { \star } = \operatorname* { m i n } _ { K } f = f ( x _ { f } )$ . For a probability measure $\xi$ on $\mathcal { F } _ { \mathrm { b l r } }$ we write $\bar { f } = \mathbb { E } _ { \xi } [ f ]$ (pointwise), a convex function $K  [ 0 , 1 ]$ which in general is not a finite convex combination of elements of ${ \mathcal { F } } _ { \mathrm { b l r } }$ (for instance the average of $x \mapsto \operatorname* { m a x } \{ \langle x , \theta \rangle , 0 \}$ over uniformly random directions in the plane is a multiple of the Euclidean norm); the arguments below only use pointwise values of ${ \bar { f } } .$

Thompson sampling. TS with prior ξ is Algorithm 1 of [1]: in round t sample $f _ { t }$ from the posterior $\mathbb { P } ( f \in \cdot \vert X _ { 1 } , Y _ { 1 } , \dots , X _ { t - 1 } , Y _ { t - 1 } )$ and play $X _ { t } = x _ { f _ { t } }$ . The observation model is Bernoulli: $Y _ { t } \in \{ 0 , 1 \}$ with $\mathbb { E } [ Y _ { t } \mid X _ { 1 } , Y _ { 1 } , \ldots , X _ { t } , f ] = f ( X _ { t } )$

Regret and information terms. Unless otherwise stated, a random function $h : K \to [ 0 , 1 ]$ is a Borel random element of $C ( K )$ (so $( \omega , x ) \mapsto h _ { \omega } ( x )$ is jointly measurable and $h ^ { \star } = \operatorname* { m i n } _ { K } h$ is measurable); laws on $\mathcal { F } _ { \mathrm { b l } }$ , and laws on $( \mathcal { F } _ { \mathrm { b l r } } , \mathcal { A } )$ through the identity map, are included. For the law ν of such a random function and a policy $\pi \in { \mathcal { P } } ( K )$ , let $( X , h ) \sim \pi \otimes \nu , \bar { h } = \mathbb { E } _ { \nu } [ h ]$ , and

$$
\Delta ( \pi , \nu ) = \mathbb { E } [ \bar { h } ( X ) - h ^ { \star } ] , \qquad I ( \pi , \nu ) = \mathbb { E } \big [ ( h ( X ) - \bar { h } ( X ) ) ^ { 2 } \big ] .
$$

For $\xi \in \mathcal { P } ( \mathcal { F } _ { \mathrm { b l r } } )$ let $\pi _ { \mathrm { T S } } ^ { \xi }$ be the law of $x _ { f }$ under $f \sim \xi$ . Following [1],

$$
\mathrm { I R } ( \mathcal { F } ) = \Big \{ ( \alpha , \beta ) \in \mathbb { R } _ { + } ^ { 2 } : \operatorname* { s u p } _ { \xi \in \mathcal { P } ( \mathcal { F } ) } \big [ \Delta ( \pi _ { \mathrm { T S } } ^ { \xi } , \xi ) - \alpha - \sqrt { \beta I ( \pi _ { \mathrm { T S } } ^ { \xi } , \xi ) } \big ] \leq 0 \Big \} .
$$

Since $\pi _ { \mathrm { T S } } ^ { \xi }$ depends on the selection, so does $\operatorname { I R } ( { \mathcal { F } } )$ ; we say $( \alpha , \beta ) \in \operatorname { I R } ( { \mathcal { F } } )$ uniformly if it holds for every measurable selection.

The decomposition lemma. The following is a restatement, for the prior mean $\bar { f } = \mathbb { E } _ { \xi } f ,$ of the decomposition device of [1, Lemma 3], which is stated there for $\bar { f } \in \mathrm { c o n v } ( \mathcal { F } )$ . Since $\mathbb { E } _ { \xi } f$ need not be a finite convex combination, we include a proof in Appendix A.

Lemma 2.1 (Decomposition; after [1, Lemma 3]). Let ${ \mathcal { F } } \subseteq { \mathcal { F } } _ { \mathrm { b l r } }$ and $\alpha , \beta _ { 0 } \geq 0$ . Suppose there are integers $k \geq 2$ and $m \geq 1$ such that for every prior $\xi \in { \mathcal { P } } ( { \mathcal { F } } )$ , with $\bar { f } = \mathbb { E } _ { \xi } f .$ , there is a measurable partition $\textstyle { \mathcal { F } } = \bigcup _ { i = 1 } ^ { m } { \mathcal { F } } _ { i }$ such that, with the maximum taken over the pieces of positive ξ-measure,

$$
\operatorname* { m a x } _ { i \in [ m ] } \Big [ \operatorname* { s u p } _ { f \in \mathcal { F } _ { i } } ( \bar { f } ( x _ { f } ) - f ^ { \star } ) - \alpha - \Big ( \beta _ { \textsc { i } , \ldots , f _ { k } \in \mathcal { F } _ { i } } \sum _ { ( j , l ) \in \mathrm { P A I R } ( k ) } \big ( f _ { j } ( x _ { f _ { l } } ) - \bar { f } ( x _ { f _ { l } } ) \big ) ^ { 2 } \Big ) ^ { 1 / 2 } \Big ] \leq 0 ,
$$

where $\mathrm { P A I R } ( k )$ is the set of ordered pairs of distinct indices. Then $( \alpha , \beta _ { 0 } k ( k - 1 ) m ) \in \mathrm { I R } ( \mathcal { F } )$ for the selection used to define $x _ { f }$

The proof averages the hypothesis over k independent draws from each piece and applies Cauchy– Schwarz over the m pieces, using $\begin{array} { r } { I ( \pi _ { \mathrm { T S } } ^ { \xi } , \xi ) \geq \sum _ { i } \xi ( \mathcal { F } _ { i } ) ^ { 2 } J _ { i } } \end{array}$ for the within-piece informations $J _ { i } ;$ only pointwise values of $\bar { f }$ are used.

Notation. $h : = d + 1$ . For a unit vector θ and a finite set C of functions with selected minimisers, $t _ { f g } = \langle x _ { g } , \theta _ { f } \rangle$ . PAIR(C) is the set of ordered pairs of distinct elements of C. Logarithms are natural.

## 3 Main results

Theorem 3.1 (Regret of TS on non-monotone convex ridge losses). Let $d \geq 1$ , let $K \subset \mathbb { R } ^ { d }$ be a convex body with $0 \in K$ and diameter D, let $\xi$ be any prior on $\mathcal { F } _ { \mathrm { b l r } }$ (with respect to any σ-algebra under which point evaluations and the selection are measurable, as in Section ${ \mathit { 2 ) } } ,$ let $f \mapsto x _ { f }$ be any such fixed measurable selection of minimisers, and let observations be Bernoulli as above. Then for every $n \geq 1$ 2

$$
\mathrm { B R e g } _ { n } ( \mathrm { T S } , \xi ) \leq 7 + 7 3 , 7 2 8 { \sqrt { 3 } } ( d + 1 ) ^ { 4 } d ^ { 1 / 2 } { \sqrt { n } } \log \left( e + n d \operatorname* { m a x } \{ 1 , D \} \right) .
$$

Theorem 3.1 combines an information-ratio bound (Theorem 3.4) with a transfer theorem (Theorem 7.5); the former reduces, through the blocking argument of [1], to a cardinality bound for the following objects.

Definition 3.2 (Uninformative configurations). Fix $c \geq 1 2 , \varepsilon > 0$ and set $\delta = \varepsilon / ( c ( d + 1 ) )$ . A configuration is a finite set $C \subset { \mathcal { F } } _ { \mathrm { b l r } }$ with pairwise distinct selected minimisers together with a convex function ${ \bar { f } } : K \to [ 0 , 1 ]$ (the comparison function) such that for all $f , g \in C$ 2

$$
r _ { f } : = \bar { f } ( x _ { f } ) - f ^ { \star } \in [ \varepsilon / 2 , \varepsilon ] , \qquad | \bar { f } ( x _ { f } ) - \bar { f } ( x _ { g } ) | \leq \delta .
$$

A row $f \in C$ is uninformative $i f \ | f ( x _ { g } ) - { \bar { f } } ( x _ { g } ) | < \delta$ for all $g \in C \setminus \{ f \}$ ; the configuration is uninformative if every row is. For fixed c and d we write $g _ { c } ( d , z )$ for the supremum of |C| over all uninformative configurations, over all convex bodies $K \ni 0$ in $\mathbb { R } ^ { d }$ , all $\varepsilon > 0$ with diam $( K ) / \varepsilon \leq z$ , all comparison functions and all selections.

Theorem 3.3 (Cardinality bound). Let $c = 9 6 ( d + 1 ) $ . Every uninformative configuration in $\mathcal { F } _ { \mathrm { b l r } }$ satisfies $| C | \leq 3 ( d + 1 ) ^ { 2 } - ( d + 1 ) - 1 < 4 ( d + 1 ) ^ { 2 } ,$ ; that is, $g _ { c } ( d , z ) \leq 3 ( d + 1 ) ^ { 2 } - ( d + 1 ) - 1 f o r$ all $z > 0$

Theorem 3.4 (Information ratio). Let $h \ : = \ : d + 1 , \ k \ : = \ : 3 0 7 2 h ^ { 4 }$ and, for $\alpha \in ( 0 , 1 ) , m _ { \alpha } =$ $1 + \lceil \log _ { 2 } ( 1 / \alpha ) \rceil$ . Then

$\left( \alpha , 1 2 k ( k - 1 ) m _ { \alpha } \right) \in \mathrm { I R } ( \mathcal { F } _ { \mathrm { b l r } } )$ uniformly over measurable selections;

in particular $\operatorname { I R } ( \mathcal { F } _ { \mathrm { b l r } } )$ contains a pair $( \alpha , \beta )$ with $\beta \leq 1 1 3 , 2 4 6 , 2 0 8 ( d + 1 ) ^ { 8 } m _ { \alpha }$

Proposition 3.5 (Tightness of the cardinality bound). For every $d \geq 2$ and every $c \geq 1 2$ there is an uninformative configuration in $\mathcal { F } _ { \mathrm { b l r } } .$ , on a truncated simplex of diameter at most $\sqrt { 2 }$ in normalised coordinates, with $| C | = d ( d + 1 )$ and $D / \varepsilon \leq 6 4 { \sqrt { 2 } } c ^ { 2 } d ( d + 1 ) ^ { 2 }$ ; rescaling the functions about the common comparison value shows that the same size is attained for every larger value of $D / \varepsilon$ . In particular, $f o r c = 9 6 ( d + 1 ) $ and z ≥ 589,824 ${ \sqrt { 2 } } d ( d + 1 ) ^ { 4 }$ $d ( d + 1 ) \leq g _ { c } ( d , z ) \leq 3 ( d + 1 ) ^ { 2 } - ( d + 1 ) - 1$ For $d = 1 , g _ { c } ( 1 , z ) \leq 2 f o r$ every $z > 0$ and $g _ { c } ( 1 , z ) = 2 ~ f o r ~ z \ge 4 .$

Proposition 3.6 (The single-removal John-ellipsoid dichotomy has no non-monotone analogue). For $d = 3$ and every $c \geq 1 2$ there is an uninformative configuration $C$ of twelve functions in $\mathcal { F } _ { \mathrm { b l r } }$ such that, writing $J _ { \zeta } ( C ) =$ conv $\{ x _ { f } : f \in C \} + B _ { \zeta }$ and $E _ { \zeta } ( \cdot )$ for the maximum-volume inscribed ellipsoid, $E _ { \zeta } ( C \setminus \{ f \} ) = E _ { \zeta } ( C )$ for every $f \in C$ and every $\zeta > 0$

Proposition 3.6 concerns the proof strategy of [1], not their results: their Lemma 8 (“some removal shrinks the John ellipsoid by a factor 0.85, or some ordered pair is informative”) is stated and proved for the monotone class only, and Proposition 3.6 shows that its literal analogue for $\mathcal { F } _ { \mathrm { b l r } }$ is false for every fixed shrink factor $\gamma < 1$ in place of 0.85. It does not exclude other uses of John ellipsoids.

## 3.1 Proof overview

The argument has five steps; we record for each its input, its output and its cost in the dimension, so that the exponent $9 / 2$ can be traced.

1. Two thin bands (Section $\it 4 )$ . Every $f \in \mathcal { F } _ { \mathrm { b l r } }$ has a representation $\ell ( \langle \cdot , \theta \rangle )$ with ℓ convex, 1- Lipschitz and globally minimised at the projections of the minimisers (Lemma 4.1). If a row $f$ of a configuration is uninformative, convexity of ℓ along $\theta _ { f }$ forces the projections of all other minimisers into two thin bands at the extreme projections, on either side of $\langle x _ { f } , \theta _ { f } \rangle$ , each of relative width $\eta \approx 6 / ( c ( d + 1 ) _ { , }$ ) (Lemma 4.2). For monotone links only one band can be non-empty; the second band is the whole dificulty.

2. Balanced and unbalanced rows (Section 5). Rows whose two band distances L, R are comparable are handled by a quadratic evaluation matrix of rank at most $h ( h + 1 ) / 2$ with small of-diagonal entries, giving at most $h ^ { 2 } - 1$ such rows (Lemma 5.1). For the remaining rows this quadratic estimate deteriorates when the two band distances are highly imbalanced, so we eliminate the far-band entries with an exactly low-rank Boolean mask instead: the near-band membership matrix H, a 0-1 matrix, is within $1 / ( 8 h )$ of an afine evaluation matrix of rank at most $h ,$ and Lemma 5.3 gives rank $H \leq 2 h - 1$ . Masking an afine matrix by H (Lemma 5.2) then yields a near-identity matrix of rank at most $h ( 2 h - 1 )$ , hence at most $2 h ^ { 2 } - h$ unbalanced rows. Altogether $| C | \le 3 h ^ { 2 } - h - 1 = O ( d ^ { 2 } )$ (Theorem 3.3), which is tight up to constants (Proposition 3.5). The rounding tolerance $1 / ( 4 r )$ with $r = h$ is what forces $c = \Theta ( d )$

3. Counting (Section 6). If a configuration has $G { + } q$ elements, deleting first endpoints of informative pairs q times shows that its ordered pairs carry energy at least $q \delta ^ { 2 }$ (Lemma 6.1). Within one dyadic level of the gap $\bar { f } ( x _ { f } ) - f ^ { \star }$ , a k-tuple is cut into $b = 4 c h$ blocks of $s = 2 G$ functions; at least half the blocks are configurations, each contributing $G \delta ^ { 2 }$ , so every k-tuple has energy at least $\varepsilon ^ { 2 } / 1 2$ with $k = b s = 3 0 7 2 h ^ { 4 }$ . Lemma 2.1 turns this into $( \alpha , 1 2 k ( k - 1 ) m _ { \alpha } ) \in \mathrm { I R } ( \mathcal { F } _ { \mathrm { b l r } } )$ i.e. $\beta = O ( d ^ { 8 } \log ( 1 / \alpha ) )$ ) (Theorem 3.4). The exponent 8 is $2 \times 4 \colon k$ is the number of blocks $\Theta ( c d ) = \Theta ( d ^ { 2 } )$ times the block size $\Theta ( G ) = \Theta ( d ^ { 2 } )$

4. Transfer (Section 7). A cover of $\mathcal { F } _ { \mathrm { b l r } }$ by $N _ { \rho } \leq ( 1 + 2 D / \rho ) ^ { d } ( 1 + 8 D / \rho ) ^ { d }$ value-based pieces, built from infimal-convolution approximations that keep the selected minimiser exact (Lemma 7.4), together with the information-ratio bound applied to finitely-valued proxies, gives $\mathrm { B R e g } _ { n } \leq$ $n \alpha + n \rho ( 6 + 4 \sqrt { \beta } ) + \sqrt { \beta n \log N _ { \rho } / 2 }$ for any fixed measurable selection (Theorem 7.5); log $N _ { 1 / n } =$ $O ( d \log ( e + n D ) )$ costs ${ \sqrt { d } } .$

5. Assembly. With $\alpha = \rho = 1 / n , \sqrt { \beta } = O ( d ^ { 4 } \sqrt { \log n } )$ and $\sqrt { \log N _ { 1 / n } } = O ( \sqrt { d \log ( e + n D ) } )$ give $\mathrm { B R e g } _ { n } = O ( d ^ { 4 } { \sqrt { d n } } \log ( e +$ nd max $\{ 1 , D \} ) = { \tilde { O } } ( d ^ { 9 / 2 } { \sqrt { n } } )$ (Theorem 3.1).

## 4 Structure of uninformative rows

Lemma 4.1 (Ridge representation with a global minimiser). For every $f \in \mathcal { F } _ { \mathrm { b l r } }$ there exist a unit vector θ and a convex, 1-Lipschitz $\ell : \mathbb { R } \to \mathbb { R }$ such that $f = \ell ( \langle \cdot , \theta \rangle )$ on $K , \ell \geq f ^ { \star } \geq 0$ on all $o f \mathbb { R }$ , and $\ell ( \left. x , \theta \right. ) = f ^ { \star } = \operatorname* { m i n } _ { \mathbb { R } } \ell$ for every minimiser x of f on K. In particular $| f ( x ) - f ( y ) | \leq | \langle \theta , x - y \rangle |$ for all $x , y \in K$

The proof (Appendix A) shows that the link is 1-Lipschitz on the projection interval of K and extends it beyond that interval with slopes ∓1; a constant extension would not be convex when the minimiser is interior. The extension is defined on all of R and attains its global minimum at the projections of the minimisers of $f ;$ the part of it outside the projection interval is used in Section 7.

Lemma 4.2 (Two thin extreme bands). Let $c \geq 1 2$ and let C be a configuration (Definition 3.2). Fix $f \in C$ with representation $( { \theta _ { f } } , { \ell _ { f } } )$ from Lemma $4 . 1 ,$ , and write $s = \langle x _ { f } , \theta _ { f } \rangle , \ t _ { g } = \langle x _ { g } , \theta _ { f } \rangle$ $a = \operatorname* { m i n } _ { g \in C } t _ { g } , b = \operatorname* { m a x } _ { g \in C } t _ { g } , L = s - a , R = b - s$ , and

$$
\eta : = \frac { 3 \delta } { \varepsilon / 2 + \delta } = \frac { 3 } { c ( d + 1 ) / 2 + 1 } .
$$

If the row f is uninformative then for every $g \neq f .$

(i) $| t _ { g } - s | > \varepsilon / 2 - 2 \delta \geq 5 \varepsilon / 1 2 ;$

(ii) $t _ { g } \in ( b - \eta R , b ] \ i f t _ { g } > s _ { \scriptscriptstyle { \mathscr { S } } }$ , and $t _ { g } \in [ a , a + \eta L ) \ i f t _ { g } < s$

The proof (Appendix A) is a two-line convexity argument: uninformativeness pins $f ( x _ { g } )$ to within δ of $\bar { f } ( x _ { g } )$ , which is at least $f ^ { \star } + \varepsilon / 2 - 2 \delta$ , giving (i); and interpolating $\ell _ { f }$ between its minimum at s and its value at the extreme projection b shows that any $t _ { g }$ strictly inside $( s , b )$ would make $f ( x _ { g } )$ smaller than $\bar { f } ( x _ { g } ) - \delta$ unless $t _ { g }$ is within relative distance η of $b ,$ giving (ii). In particular, along its own direction each uninformative row sees the other minimisers at one of two projected distances, L and $R ,$ each determined up to a relative error η.

## 5 The cardinality bound

## 5.1 Three linear-algebra tools

Lemma 5.1 (Trace–rank bound: the classical symmetric bound of $[ 8 ,$ Lemma 2.2] and its non-symmetric extension). For any real $N \times N$ matrix M, $| \mathrm { t r } M | ^ { 2 } \leq$ rank $( M ) \| M \| _ { F } ^ { 2 }$ . Consequently, if $M _ { i i } = 1$ for all i, $| M _ { i j } | \le t$ for $i \neq j$ , rank $M \leq Q$ and $Q t ^ { 2 } < 1$ , then

$$
N \leq \frac { Q ( 1 - t ^ { 2 } ) } { 1 - Q t ^ { 2 } } \leq \frac { Q } { 1 - Q t ^ { 2 } } ;
$$

and if moreover $Q \geq 1$ is an integer and $t < 1 / Q$ , then $N \leq Q$

For symmetric M the second claim is [8, Lemma 2.2]; the trace–rank inequality for arbitrary real matrices also appears as [7, Fact 10]. The short proof (via the orthogonal projection onto the column space of M) is in Appendix B.

Lemma 5.2 (Hadamard products). For real matrices $U , V$ of the same shape, rank $( U \circ V ) \leq$ $\operatorname { r a n k } ( U ) \operatorname { r a n k } ( V )$ , where ◦ is the entrywise product.

Proof. Write $\begin{array} { r } { U = \sum _ { a \leq p } u _ { a } v _ { a } ^ { \top } } \end{array}$ and $\begin{array} { r } { V = \sum _ { b \leq q } y _ { b } z _ { b } ^ { \top } } \end{array}$ with $p = { \mathrm { r a n k } } U$ $q = { \mathrm { r a n k } } V ;$ ; then $U \circ V =$ $\begin{array} { r } { \sum _ { a , b } ( u _ { a } \circ y _ { b } ) ( v _ { a } \circ z _ { b } ) ^ { \top } } \end{array}$ □

Lemma 5.3 (Integer rounding preserves rank up to a factor two). Let $r \geq 1$ be an integer, let A be a real matrix with rank $A \leq r$ , and let B be a 0-1 matrix of the same shape with $\| A - B \| _ { \operatorname* { m a x } } \leq \gamma$ $I f \gamma \leq 1 / ( 4 r )$ then rank $B \leq 2 r - 1$

Proof sketch. If rank $B \geq 2 r$ , take a non-singular $2 r \times 2 r$ submatrix $B _ { 0 }$ of B, the corresponding $A _ { 0 } ,$ and $E = B _ { 0 } - A _ { 0 }$ , so rank $A _ { 0 } \leq r$ and $\| E \| _ { F } ^ { 2 } \le 4 r ^ { 2 } \gamma ^ { 2 }$ . On an r-dimensional subspace of ker $A _ { 0 }$ the matrix $B _ { 0 }$ coincides with E, so the r smallest squared singular values of $B _ { 0 }$ sum to at most $4 r ^ { 2 } \gamma ^ { 2 }$ while the r largest sum to at most $\| B _ { 0 } \| _ { F } ^ { 2 } \le 4 r ^ { 2 } - 1$ (a non-singular 0-1 matrix has a zero entry). The arithmetic–geometric mean inequality on each group gives | det $B _ { 0 } | \leq ( 4 r \gamma ) ^ { r } ( 1 - 1 / ( 4 r ^ { 2 } ) ) ^ { r / 2 } < 1$ for $\gamma \leq 1 / ( 4 r )$ , contradicting | det $| B _ { 0 } | \ge 1$ for a non-singular integer matrix. The details are in Appendix B.

We have not found this elementary statement in the literature in this form, but we do not claim priority for it; the approximate-rank literature (e.g. [8, 9]) studies both upper and lower bounds on the ε-approximate rank of real matrices and their algorithmic applications, and we use the elementary rounding statement above only in its specific role, namely to turn a band-membership matrix, which is only approximately low-rank, into an exactly low-rank 0-1 mask; the integrality of the mask is what allows the two bands of Lemma 4.2 to be handled without any control on the ratio of their distances. The tolerance $1 / ( 4 r )$ cannot be raised to order $1 / \sqrt { r }$ for arbitrary 0-1 matrices (Proposition E.1 in Appendix E).

## 5.2 Proof of Theorem 3.3

Let $h = d + 1 , c = 9 6 h$ , so that

$$
\eta = \frac { 3 } { 4 8 h ^ { 2 } + 1 } < \frac { 1 } { 1 6 h ^ { 2 } } , \qquad \delta = \frac { \varepsilon } { 9 6 h ^ { 2 } } .\tag{1}
$$

Let $C$ be an uninformative configuration with $N = | C | \geq 3$ (for $N \leq 2$ there is nothing to prove). For each row $i \in C$ take the representation of Lemma 4.1 and the quantities $t _ { i j } = \langle x _ { j } , \theta _ { i } \rangle , s _ { i } , a _ { i } , b _ { i }$

$L _ { i } , R _ { i }$ of Lemma 4.2. By Lemma $4 . 2 ( \mathrm { i } ) , L _ { i } + R _ { i } > 0$ . Replacing $( \theta _ { i } , \ell _ { i } )$ by $\left( - \theta _ { i } , \ell _ { i } ( - \cdot ) \right)$ if necessary (this changes neither the function nor its selected minimiser), we may assume

$$
0 \leq L _ { i } \leq R _ { i } , \qquad R _ { i } > 0 , \qquad w _ { i } : = L _ { i } + R _ { i } , \qquad \lambda _ { i } : = L _ { i } / w _ { i } \in [ 0 , 1 / 2 ] .
$$

By Lemma 4.2(ii) every $j \neq i$ is either in the near band $t _ { i j } \in [ a _ { i } , a _ { i } + \eta L _ { i } )$ (empty if $L _ { i } = 0 )$ or in the $f a r$ band $t _ { i j } \in ( b _ { i } - \eta R _ { i } , b _ { i } ]$ . Throughout, $a _ { i } , b _ { i } , s _ { i } , L _ { i } , R _ { i }$ refer to the whole configuration $C$ and are never recomputed for subsets. Split

$$
I _ { B } = \{ i : \lambda _ { i } > 1 / ( 8 h ) \} , \qquad I _ { U } = \{ i : \lambda _ { i } \leq 1 / ( 8 h ) \} .
$$

Balanced rows. For $i \in I _ { B }$ we have $L _ { i } R _ { i } > 0$ and $R _ { i } / L _ { i } < 8 h - 1$ . Consider the quadratic polynomial $p _ { i } ( x ) = ( \langle x , \theta _ { i } \rangle - a _ { i } ) ( b _ { i } - \langle x , \theta _ { i } \rangle ) / ( L _ { i } R _ { i } )$ and the matrix $P = ( p _ { i } ( x _ { j } ) ) _ { i , j \in I _ { B } }$ . Then $P _ { i i } = 1$ , and for $j \neq i$ in the near band $0 \leq P _ { i j } < \eta ( L _ { i } + R _ { i } ) / R _ { i } \leq 2 \eta$ , in the far band $0 \leq P _ { i j } <$ 2 $\eta ( L _ { i } + R _ { i } ) / L _ { i } < 8 h \eta ;$ so all of-diagonal entries are below $t _ { B } : = 8 h \eta < 1 / ( 2 h )$ . Each row of P is a polynomial of total degree at most two in $x _ { j } \in \mathbb { R } ^ { d }$ , so rank $P \leq Q _ { B } : = 1 + d + d ( d + 1 ) / 2 = h ( h + 1 ) / 2$ Since $Q _ { B } t _ { B } ^ { 2 } < ( h + 1 ) / ( 8 h ) \leq 3 / 1 6$ , Lemma 5.1 gives

$$
| I _ { B } | \leq { \frac { Q _ { B } } { 1 - Q _ { B } t _ { B } ^ { 2 } } } < { \frac { 4 h ^ { 2 } ( h + 1 ) } { 7 h - 1 } } < h ^ { 2 } , \qquad { \mathrm { i . e . ~ } } | I _ { B } | \leq h ^ { 2 } - 1 ,\tag{2}
$$

where the last strict inequality uses $7 h - 1 - 4 ( h + 1 ) = 3 h - 5 > 0 { \mathrm { ~ f o r ~ } } h \geq 2 .$

Unbalanced rows. All matrices below are indexed by $I _ { U } \times I _ { U }$ . Define the 0-1 matrix H by $H _ { i i } = 1$ and, for $j \neq i , H _ { i j } = 1 \ \mathrm { i f } \ t _ { i j }$ lies in the near band of row i and $H _ { i j } = 0$ if it lies in the far band. Define the afine evaluation matrix $A _ { i j } = ( b _ { i } - t _ { i j } ) / w _ { i } ;$ each row is an afine function of $x _ { j }$ , so rank $A \leq h$ . Entrywise, $| A _ { i i } - 1 | = \lambda _ { i } \leq 1 / ( 8 h )$ ; for j in the near band $| A _ { i j } - 1 | = ( t _ { i j } - a _ { i } ) / w _ { i } < \eta \lambda _ { i } ;$ for j in the far band $| A _ { i j } | = ( b _ { i } - t _ { i j } ) / w _ { i } < \eta ( 1 - \lambda _ { i } )$ . By $( 1 ) , \| A - H \| _ { \operatorname* { m a x } } \leq 1 / ( 8 h )$ , so Lemma 5.3 with $r = h$ gives rank $H \leq 2 h - 1$

Next define $U _ { i j } = ( t _ { i j } - a _ { i } ) / L _ { i }$ if $L _ { i } > 0$ and $U _ { i j } = 1 \mathrm { i f } \ L _ { i } = 0 ;$ again each row is afine in $x _ { j }$ , so rankU ≤ h. Let $M = U \circ H$ . Then $M _ { i i } = 1$ ; for j in the far band $M _ { i j } = 0$ exactly (the mask removes the possibly huge values $U _ { i j }$ there); for j in the near band $0 \leq M _ { i j } < \eta ;$ and rows with $L _ { i } = 0$ have no near band, so their of-diagonal entries vanish. By Lemma 5.2, rank $M \leq h ( 2 h - 1 ) = : Q _ { U }$ , and $Q _ { U } \eta < h ( 2 h - 1 ) / ( 1 6 h ^ { 2 } ) < 1 / 8$ , so $\eta < 1 / Q _ { U }$ and the integer form of Lemma 5.1 gives

$$
| I _ { U } | \le Q _ { U } = 2 h ^ { 2 } - h .\tag{3}
$$

Conclusion. By (2) and (3), $| C | = | I _ { B } | + | I _ { U } | \le 3 h ^ { 2 } - h - 1 < 4 h ^ { 2 } .$

Only Lemmas 4.1 and 4.2 and finite-dimensional linear algebra are used: no monotonicity, no John ellipsoid, no bound on $D / \varepsilon$ , and only the fact that $x _ { f }$ is some minimiser, so the bound holds for every measurable selection. On the family of Proposition 3.5 every row is unbalanced, H is the “same base vertex” mask, and M is the identity.

## 6 From the cardinality bound to the information ratio

Lemma 6.1 (Counting informative pairs). Fix $c \geq 1 2$ and suppose every uninformative configuration (Definition 3.2, with this $^ { c ) }$ has at most G elements. Let $q \geq 1$ be an integer. $I f C$ satisfies the hypotheses of Definition 3.2 and $| C | \geq G + q$ , then

$$
\sum _ { ( f , g ) \in \mathrm { P A I R } ( C ) } \left( f ( x _ { g } ) - \bar { f } ( x _ { g } ) \right) ^ { 2 } \geq q \delta ^ { 2 } .
$$

The proof (Appendix B) deletes, q times, the first endpoint of an ordered pair with $\mathinner { | { f ( x _ { g } ) - } }$ $\bar { f } ( x _ { g } ) | \geq \delta ;$ such a pair exists while more than G elements remain, the hypotheses of Definition 3.2 survive deletion, and the $q$ recorded pairs have distinct first endpoints.

Proof of Theorem $\ 3 . 4$ . Fix a measurable selection, a prior ξ on $\mathcal { F } _ { \mathrm { b l r } }$ , and $\bar { f } = \mathbb { E } _ { \xi } f$ . Let $h = d + 1$ $c = 9 6 h$ and $G = 4 h ^ { 2 } ;$ by Theorem 3.3 every uninformative configuration for this c has at most G elements. With $\varepsilon _ { i } = \alpha 2 ^ { i }$ partition $\mathcal { F } _ { \mathrm { b l r } }$ into ${ \mathcal { F } } _ { 0 } = \{ f : { \bar { f } } ( x _ { f } ) - f ^ { \star } \leq \alpha \}$ and $\mathcal { F } _ { i } = \{ f : \bar { f } ( x _ { f } ) - f ^ { \star } \in$ $( \alpha 2 ^ { i - 1 } , \alpha 2 ^ { i } ] \}$ for $1 \leq i \leq \lceil \log _ { 2 } ( 1 / \alpha ) \rceil$ ; these are measurable (they only involve $f \mapsto { \bar { f } } ( x _ { f } )$ and $f \mapsto f ^ { \star } )$ and there are $m _ { \alpha }$ of them. The condition of Lemma 2.1 holds trivially on $\mathcal { F } _ { 0 }$ . Fix $i \geq 1$ , $\varepsilon = \varepsilon _ { i } , \delta = \varepsilon / ( c h )$ , and set

$$
b = 4 c h , \qquad s = 2 G , \qquad k = b s = 8 c h G = 3 0 7 2 h ^ { 4 }
$$

(over $b = 2 c h + u , s = G + v$ the argument below yields energy $u v \delta ^ { 2 }$ , and $k ^ { 2 } / ( u v )$ is minimised at $u = 2 c h , v = G )$ . Let $f _ { 1 } , \ldots , f _ { k } \in { \mathcal { F } } _ { i }$ be a k-tuple. We show

$$
S : = \sum _ { ( j , l ) \in \mathrm { P A I R } ( k ) } \left( f _ { j } ( x _ { f _ { l } } ) - \bar { f } ( x _ { f _ { l } } ) \right) ^ { 2 } \geq \frac { \varepsilon ^ { 2 } } { 1 2 } ,\tag{4}
$$

which with su $) _ { f \in \mathscr { F } _ { i } } ( \bar { f } ( x _ { f } ) - f ^ { \star } ) \leq \varepsilon \leq \sqrt { 1 2 S }$ and Lemma 2.1 (applied with $\beta _ { 0 } = 1 2 )$ proves the theorem. If two functions of the tuple share the selected minimiser (in particular if they coincide), the pair contributes $( \bar { f } ( x _ { f } ) - f ^ { \star } ) ^ { 2 } \ge \varepsilon ^ { 2 } / 4$ . Otherwise order the tuple so that $v _ { j } = \bar { f } ( x _ { f _ { j } } )$ is non-increasing. If $v _ { 1 } - v _ { k } \ge 2 \varepsilon$ then $f _ { 1 } ( x _ { f _ { k } } ) \geq f _ { 1 } ^ { \star } \geq v _ { 1 } - \varepsilon \geq v _ { k } + \varepsilon$ and the pair $( 1 , k )$ contributes $\varepsilon ^ { 2 }$ . Otherwise cut the ordered tuple into b consecutive blocks of s functions; the sum of the value spreads of the blocks is less than $2 \varepsilon$ , so fewer than $2 \varepsilon / \delta = 2 c h$ blocks have spread larger than $\delta ,$ and at least $b - 2 c h = 2 c h$ blocks have spread at most $\delta .$ . Each such block, together with ${ \bar { f } } ,$ satisfies Definition 3.2 (its elements have $r _ { f } \in ( \varepsilon / 2 , \varepsilon ]$ and distinct minimisers) and has $s = 2 G = G + G$ elements, so Lemma 6.1 with $q = G$ shows that it contributes at least $G \delta ^ { 2 }$ ; the blocks are disjoint, so

$$
S \geq 2 c h \cdot G \delta ^ { 2 } = \frac { 2 G } { c h } \varepsilon ^ { 2 } = \frac { 8 h ^ { 2 } } { 9 6 h ^ { 2 } } \varepsilon ^ { 2 } = \frac { \varepsilon ^ { 2 } } { 1 2 } .
$$

Since $\varepsilon ^ { 2 } / 4$ and $\varepsilon ^ { 2 }$ are not smaller, (4) holds for every tuple. Lemma 2.1 gives $( \alpha , 1 2 k ( k - 1 ) m _ { \alpha } ) \in$ $\operatorname { I R } ( \mathcal { F } _ { \mathrm { b l r } } )$ , and $1 2 k ^ { 2 } = 1 2 \cdot 3 0 7 2 ^ { 2 } h ^ { 8 } = 1 1 3 , 2 4 6 , 2 0 8 h ^ { 8 } .$ . Nothing in the argument depends on which measurable selection was fixed. □

In [1] the role of Lemma 6.1 is played by their Lemma 9, which iterates their John-ellipsoid dichotomy (Lemma 8); Proposition 3.6 shows that no such dichotomy holds for $\mathcal { F } _ { \mathrm { b l r } }$ , which is why we count through a cardinality bound instead.

## 7 From the information ratio to regret under a fixed selection rule

The transfer theorem of [1, Theorem 28] bounds the regret of (approximate) TS from an informationratio bound and a cover of the function class, and its covering argument for ridge classes uses, as the authors note, that ties among minimisers are broken consistently. For ${ \mathcal { F } } _ { \mathrm { b l t } }$ <sub>r</sub> minimisers can be nonunique (an interior minimising projection level produces a positive-dimensional minimising section when $d \geq 2 )$ , so we give a self-contained transfer theorem for exact TS that is valid for any fixed measurable selection rule. Two ingredients difer from [1]: the cover is built from infimal-convolution approximations, so that the selected minimiser of $f$ is an exact minimiser of its approximation (Lemma 7.4); and the information-ratio hypothesis is required uniformly over selection rules and extended to finitely-supported randomised minimisers (Lemma 7.1), which is what the proof actually uses. The three preparatory lemmas below are proved in Appendix C.

Lemma 7.1 (Uniform bounds extend to randomised minimisers). Suppose $( \alpha , \beta ) \in \operatorname { I R } ( { \mathcal { F } } )$ for every measurable selection, where ${ \mathcal { F } } \subseteq { \mathcal { F } } _ { \mathrm { b l r } }$ . Let $\xi \in { \mathcal { P } } ( { \mathcal { F } } )$ and let $( f , X )$ be a random pair with $f \sim \xi$ and $X \in \arg \operatorname* { m i n } _ { K } f$ almost surely, given by a measurable kernel. Then $\Delta ( \mathcal { L } ( X ) , \xi ) \leq \alpha + \sqrt { \beta I ( \mathcal { L } ( X ) , \xi ) }$

Lemma 7.1 is where uniformity over selections is used: a randomised minimiser is a mixture of measurable selections (by Kallenberg’s randomisation lemma), and both $\Delta$ and I are linear in the policy.

Lemma 7.2 (Continuity and near-minimisers). (a) Let $f , g$ be Borel random elements of $C ( K )$ with values in [0, 1] (no convexity or ridge assumption), coupled with $\| f - g \| _ { \infty } \leq \rho \ a . s .$ , and let $\pi$ be independent of them. Then $\sqrt { I ( \pi , \mathcal { L } ( g ) ) } \leq \sqrt { I ( \pi , \mathcal { L } ( f ) ) } + \rho$

(b) Let ν be the law of a Borel random element of C(K) that is a.s. 1-Lipschitz with values in $[ 0 , 1 ]$ and let actions $X , Z$ be coupled with $\| X - Z \| \leq \rho$ a.s. and independent of the function. Then $\sqrt { I ( \mathcal L ( Z ) , \nu ) } \leq \sqrt { I ( \mathcal L ( X ) , \nu ) } + \rho .$

(c) Suppose $( \alpha , \beta ) \in \mathrm { I R } ( \mathcal { F } _ { \mathrm { b l r } } )$ uniformly over selections. Let $( f , X )$ be a random pair taking finitely many values, with $f \in { \mathcal { F } } _ { \mathrm { b l r } }$ and $f ( X ) \leq f ^ { \star } + \rho \ a . s .$ . Then $\Delta ( \mathcal { L } ( X ) , \mathcal { L } ( f ) ) \leq \alpha +$ $\sqrt { \beta I ( \mathcal { L } ( X ) , \mathcal { L } ( f ) ) } + \rho ( 1 + \sqrt { \beta } )$

Parts (a) and (b) say that $\sqrt { I }$ is 1-Lipschitz under uniform perturbations of the function and, for Lipschitz functions, of the action; both are proved with a single conditional-centring operator, which is an $L ^ { 2 }$ contraction. Part (c) passes from near-minimisers to exact minimisers by flattening $f$ at the level $f ( X )$ ; since $I ( \pi , \nu )$ depends only on the marginal laws of the policy and of the random function, part (a) can then be applied on a product coupling with an independent copy of $X ,$ even though X and f are dependent. Finite support is assumed so that the flattened proxy is a finitely-valued, hence A-measurable, kernel.

Lemma 7.3 (Measurable representations). There are a Borel map $f \mapsto \theta _ { f } \in S ^ { d - 1 }$ on $( \mathcal { F } _ { \mathrm { b l r } } , B )$ and a jointly continuous map $( f , \theta , t ) \mapsto L ( f , \theta , t )$ on $\mathcal { F } _ { \mathrm { b l r } } \times S ^ { d - 1 } \times \mathbb { R }$

$$
L ( f , \theta , t ) = \operatorname* { m i n } _ { y \in K } \big [ f ( y ) + | t - \langle \theta , y \rangle | \big ] ,
$$

such that, for every $f , ( \theta _ { f } , L ( f , \theta _ { f } , \cdot ) )$ is a representation of f as in Lemma $\it 4 . 1$

The direction map exists by the Kuratowski–Ryll-Nardzewski selection theorem, applied to the closed-valued, weakly measurable correspondence $f \mapsto \{ \theta : | f ( x ) - f ( y ) | \leq | \langle \theta , x - y \rangle | \forall x , y \}$ on the compact set ${ \mathcal { F } } _ { \mathrm { b l r } } \subset C ( K ) ;$ L is the infimal convolution of f with $\left| t - \langle \theta , \cdot \rangle \right|$

Lemma 7.4 (Cover by infimal-convolution approximations). Let $0 < \rho < 1$ , and choose a ρ-net $C _ { K }$ of K and a $\rho / ( 2 D )  – n e t C _ { S } \ o f \ S ^ { d - 1 }$ with $| C _ { K } | \leq ( 1 + 2 D / \rho ) ^ { d }$ and $| C _ { S } | \le ( 1 + 8 D / \rho ) ^ { d }$ (such nets exist by the standard volume argument), so $N _ { \rho } : = | C _ { K } | | C _ { S } | \le ( 1 + 2 D / \rho ) ^ { d } ( 1 + 8 D / \rho ) ^ { d }$ . For $( x , \theta ) \in C _ { K } \times C _ { S }$ let

$\mathcal { F } _ { x , \theta } ^ { \mathrm { v a l } } = \{ g \in \mathcal { F } _ { \mathrm { b l r } } : \ g$ is a ridge function in direction θ, $g ( x ) \leq g ^ { \star } + 2 \rho \}$

Then $( i )$ every probability average of functions in $\mathcal { F } _ { x , \theta } ^ { \mathrm { v a l } }$ lies in $\mathcal { F } _ { \mathrm { b l r } }$ , is a ridge function in direction $\theta ,$ , and is 2ρ-near-minimal at $x ; \ ( i i )$ for every measurable selection there are measurable maps $f \mapsto \kappa ( f ) = ( x _ { \kappa } , \theta _ { \kappa } ) \in C _ { K } \times C _ { S }$ and $f \mapsto g _ { f } \in \mathcal { F } _ { \kappa ( f ) } ^ { \mathrm { v a l } }$ with $\| f - g _ { f } \| _ { \infty } \leq \rho / 2 , \| x _ { f } - x _ { \kappa } \| \leq \rho _ { \kappa }$ , and such that x<sub>f</sub> is an exact minimiser of $g _ { f }$ with $g _ { f } ^ { \star } = f ^ { \star }$

Proof idea. (i) is closed under averaging because all functions in a piece share the direction $\theta ,$ and near-minimality at x is preserved since the minimum of an average is at least the average of the minima. For (ii), let $\theta _ { f }$ and L be as in Lemma 7.3, let $\kappa ( f )$ be the first pair in a fixed enumeration of $C _ { K } \times C _ { S }$ with $\| x _ { \kappa } - x _ { f } \| \le \rho$ and $\lVert \theta _ { \kappa } - \theta _ { f } \rVert \leq \rho / ( 2 D )$ , and set $g _ { f } ( x ) = L ( f , \theta _ { \kappa } , \langle x , \theta _ { \kappa } \rangle ) =$ $\mathrm { m i n } _ { y \in K } [ f ( y ) + | \langle \theta _ { \kappa } , x - y \rangle | ]$ , the infimal convolution of f with the ridge kernel $| \langle \theta _ { \kappa } , \cdot \rangle |$ |. Its profile is convex and 1-Lipschitz, so $g _ { f }$ is a ridge function in direction $\theta _ { \kappa } ; \boldsymbol { y } = \boldsymbol { x }$ gives $g _ { f } \leq f$ and $f \geq f ^ { \star }$ gives $g _ { f } \geq f ^ { \star }$ , so no rescaling is needed; the inequality $f ( x ) - f ( y ) \leq | \langle \theta _ { f } , x - y \rangle |$ of Lemma 4.1 and $\lVert \theta _ { \kappa } - \theta _ { f } \rVert \leq \rho / ( 2 D )$ give $f - \rho / 2 \leq g _ { f } ;$ and $f ^ { \star } \le g _ { f } ( x _ { f } ) \le f ( x _ { f } ) = f ^ { \star }$ shows that $x _ { f }$ is an exact minimiser of $g _ { f }$ . The details, including measurability, are in Appendix C.

Theorem 7.5 (Transfer under any fixed measurable selection rule). Suppose $( \alpha , \beta ) \in \mathrm { I R } ( \mathcal { F } _ { \mathrm { b l r } } )$ uniformly over measurable selections. Then for every measurable selection, every prior ξ on $\mathcal { F } _ { \mathrm { b l r } }$ every $n \geq 1$ and every $\rho \in ( 0 , 1 )$ , exact Thompson sampling satisfies

$$
\begin{array} { r } { \mathrm { B R e g } _ { n } ( \mathrm { T S } , \xi ) \le n \alpha + n \rho ( 6 + 4 \sqrt { \beta } ) + \sqrt { \frac { 1 } { 2 } \beta n \log N _ { \rho } } . } \end{array}
$$

Proof idea. Fix a round, let $f \sim \xi$ be the current posterior, $X ^ { \star } = x _ { f } ,$ , and let X be an independent copy of $X ^ { \star }$ , so ${ \mathcal { L } } ( X ) = \pi _ { \mathrm { T S } }$ . With $\kappa = \kappa ( f ) , g = g _ { f }$ and $z = x _ { \kappa }$ from Lemma 7.4, set $H _ { \kappa } = \mathbb { E } [ f \mid \kappa ]$ and $G _ { \kappa } \ : = \ : \mathbb { E } [ g \mid \kappa ]$ Both take finitely many values (one per label). $G _ { \kappa }$ is a finitely-valued $( \mathcal { F } _ { \mathrm { b l r } } , \mathcal { A } )$ -valued random element, a ridge function in direction $\theta _ { \kappa }$ that is 2ρ-near-minimal at $z$ (Lemma $7 . 4 ( \mathrm { i } ) ) ; H _ { \kappa }$ is only a convex 1-Lipschitz function in $\mathcal { F } _ { \mathrm { b l } }$ (an average of ridge functions with nearby but diferent directions is in general not a ridge function), viewed as a Borel $C ( K )$ -valued element, and $\| G _ { \kappa } - H _ { \kappa } \| _ { \infty } \leq \rho / 2$ . The information-ratio hypothesis is applied, via Lemmas 7.1 and $7 . 2 ( \mathrm { c } )$ , only to the finitely-valued flattened proxy max $\{ G _ { \kappa } , G _ { \kappa } ( z ) \}$ , never to $H _ { \kappa }$ . Since $g \leq f ,$ $\| f - g \| _ { \infty } \leq \rho / 2$ and $\| X ^ { \star } - z \| \leq \rho$ , the one-step regret satisfies $\Delta ( \pi _ { \mathrm { T S } } , \xi ) \le \Delta ( \pi _ { z } , \nu _ { G } ) + { \textstyle \frac { 5 } { 2 } } \rho$ with $\nu _ { G } = \mathcal { L } ( G _ { \kappa } )$ and $\pi _ { z } = \mathcal { L } ( z )$ ; Lemma $7 . 2 ( \mathrm { c } )$ gives $\Delta ( \pi _ { z } , \nu _ { G } ) \leq \alpha + \sqrt { \beta I ( \pi _ { z } , \nu _ { G } ) } + 2 \rho ( 1 + \sqrt { \beta } )$ ; and Lemma $7 . 2 ( \mathrm { a } ) , ( \mathrm { b } )$ , applied on a product space built from two independent posterior draws, give $\sqrt { I ( \pi _ { z } , \nu _ { G } ) } \leq \sqrt { I ( \pi _ { \mathrm { T S } } , \mathcal { L } ( H _ { \kappa } ) ) } + \frac { 3 } { 2 } \rho$ . Altogether $\Delta ( \pi _ { \mathrm { T S } } , \xi ) \leq \alpha + \sqrt { \beta I ( \pi _ { \mathrm { T S } } , \mathcal { L } ( H _ { \kappa } ) ) } + \rho ( 6 + 4 \sqrt { \beta } )$ . For Bernoulli observations $\begin{array} { r } { I ( \pi _ { \mathrm { T S } } , \mathcal { L } ( \bar { H _ { \kappa } } ) ) \leq \frac { 1 } { 2 } \mathsf { I } ( \kappa ; X , Y ) } \end{array}$ by Pinsker’s inequality; the label $\kappa ( f )$ is a fixed function of the environment, so the chain rule for mutual information gives $\begin{array} { r } { \mathbb { E } \sum _ { t } \mathsf { I } _ { t - 1 } ( \kappa ; X _ { t } , Y _ { t } ) \leq } \end{array}$ log $N _ { \rho } ,$ , and Cauchy–Schwarz over rounds finishes. The full proof is in Appendix C.

Proof of Theorem 3.1. For $n = 1$ the regret is at most $1 \leq 7 .$ . For $n \geq 2$ take $\alpha = \rho = 1 / n$ and $L = \log ( e + n d \operatorname* { m a x } \{ 1 , D \} )$ : Theorem 3.4 gives ${ \sqrt { \beta } } \leq 1 2 , 2 8 8 { \sqrt { 3 } } ( d + 1 ) ^ { 4 } { \sqrt { L } }$ (using $m _ { 1 / n } \leq 4 L )$ Lemma 7.4 gives log $N _ { 1 / n } \leq 8 d L$ , and Theorem 7.5 yields $\mathrm { B R e g } _ { n } \leq 7 + 6 { \sqrt { \beta } } { \sqrt { n d L } } = 7 + 7 3 , 7 2 8 { \sqrt { 3 } } ( d + { }$ $1 ) ^ { 4 } d ^ { 1 / 2 } \sqrt { n } L \mathrm { : }$ ; the arithmetic is in Appendix C.

## 8 The lower-bound family and the failure of the John dichotomy

The proofs of Propositions 3.5 and 3.6 are in Appendix D; we describe the constructions. For Proposition 3.5 take the standard d-simplex with vertices $v _ { 0 } , \ldots , v _ { d } .$ a small $\tau = 1 / ( 4 c ( d + 1 ) )$ , and the $d ( d + 1 )$ points $x _ { i j } = ( 1 - \tau ) v _ { i } + \tau v _ { j } \ ( i \neq j )$ , which are the vertices of a truncated simplex $K$ The loss $f _ { i j }$ is a V-shaped ridge function of the afine coordinate $u _ { i j } = 1 - \lambda _ { i } + \tau \lambda _ { j }$ (barycentric coordinates $\lambda )$ , with a steep left slope and a shallow right slope, minimised at $x _ { i j }$ and equal to the constant comparison function $\bar { f } \equiv 1 / 4$ at every other point up to a cross error below $\delta ;$ the gap is $\varepsilon = \tau ^ { 2 } / ( 4 d )$ . Along its own direction, row $( i , j )$ sees the $d - 1$ points $x _ { i l }$ with the same base vertex in a near band at u-distance $\tau ^ { 2 }$ and the other $d ^ { 2 }$ points in a far band at u-distance about 1; every row is two-sided, and the ratio of the band distances is of order $1 / \tau ^ { 2 }$ , unbounded in the family. For Proposition 3.6 take $d = 3$ and the regular tetrahedron: the twelve points $x _ { i j }$ are the vertices of a truncated tetrahedron $P ,$ removing any one vertex cuts P by a plane through its three neighbours, and both P and the cut body still contain the inscribed ball of the tetrahedron and are contained in the tetrahedron enlarged by the smoothing radius; an arithmetic–geometric mean bound on the determinant of any inscribed ellipsoid shows that the maximum-volume inscribed ellipsoid of both bodies is that ball, so no single removal shrinks it.

In the monotone case an uninformative row has all other minimisers on one side of its own along the ridge direction, and [1] show that they then lie beyond the centre of the John ellipsoid, so removing the row cuts the ellipsoid through its centre; for non-monotone links both bands of Lemma 4.2 can be non-empty and arbitrarily imbalanced, and Proposition 3.6 shows that the single-removal, fixed-shrink-factor dichotomy cannot hold. We do not obtain the required uniform cardinality estimate from polynomial approximation (a univariate polynomial that is 1 at the row’s own projection and small on both bands); the mask argument of Lemma 5.3 avoids the need to control the imbalance ratio of the two band distances.

## 9 Discussion

Theorem 3.1 resolves the qualitative question of [1]: monotonicity of the link is not needed for exact-posterior Thompson sampling to achieve Bayesian regret polynomial in d and $\sqrt { n }$ in $n .$ . The quantitative question of the dimension dependence remains open: the information-ratio certificate we construct has size $O ( ( d + 1 ) ^ { 8 } ( 1 + \log ( 1 / \alpha ) ) )$ against ${ \cal \tilde { O } } ( d ^ { 4 } )$ in the monotone analysis (this is the size of our certificate, not a lower bound on the best information ratio), and the resulting regret exponent is $9 / 2$ against $5 / 2$ . The loss comes from two places. The cardinality bound $\Theta ( d ^ { 2 } )$ is tight (Proposition 3.5), so the number of functions per block in Lemma 2.1 is $\Theta ( d ^ { 2 } )$ and the number of blocks is $\Theta ( c d ) ;$ ; and the constant $c = 9 6 ( d + 1 ) $ is dictated by the rounding threshold $1 / ( 4 r )$ of Lemma 5.3, which forces $\eta \lesssim 1 / d ^ { 2 }$ . The order $1 / r$ of this tolerance cannot be improved to order $1 / \sqrt { r }$ for arbitrary 0-1 matrices: Proposition E.1 (Appendix E) exhibits, for every $r = 2 ^ { j } - 1$ with $j \geq 2$ , a rank-r matrix within $2 / ( r + 1 )$ in max-norm of a 0-1 matrix of rank $2 r$ . This rules out only a better uniform rounding theorem for arbitrary 0-1 matrices; the band-membership matrices of Section 5 have additional structure, and any improvement of the exponent through a larger rounding tolerance would have to exploit it. We do not claim that the exponent $9 / 2$ is optimal. Whether the monotone exponent $5 / 2$ can be reached, and what the correct dimension dependence of TS on this class is (no lower bound specific to non-monotone links is known), are left open. Three remarks on scope. (1) The statement is about Bayesian regret with an arbitrary prior; it does not give a frequentist guarantee for every fixed environment, and it concerns exact-posterior TS without addressing computation, which remains open. (2) The selection rule is arbitrary but fixed in advance; rules that change with the history are not covered. (3) The observation model is Bernoulli; the only place the model enters is Pinsker’s inequality for the conditional means of [0, 1]-valued observations, so the same constants hold for observations $Y _ { t } \in [ 0 , 1 ]$ satisfying $\mathbb { E } [ Y _ { t } \mid { \mathcal { H } } _ { t - 1 } , X _ { t } , f ] = f ( X _ { t } )$ almost surely, where $\mathcal { H } _ { t - 1 } = \sigma ( X _ { 1 } , Y _ { 1 } , \ldots , X _ { t - 1 } , Y _ { t - 1 } )$ , provided that a complete observation model is specified under which the posterior is well defined and TS uses fresh internal randomness conditionally on the history (the condition $\mathbb { E } [ Y _ { t } \mid X _ { t } , f ] = f ( X _ { t } )$ alone is not suficient). The second question in the same paragraph of [1], the information ratio for a known convex link, is not touched here; see also the MSc thesis of Bakhtiari [2].

## References

[1] A. Bakhtiari, T. Lattimore, and Cs. Szepesv´ari. Thompson sampling for bandit convex optimisation. In Proceedings of the 38th Conference on Learning Theory (COLT), PMLR 291:231–263, 2025.

[2] A. Bakhtiari. Thompson Sampling for Bandit Convex Optimization. MSc thesis, University of Alberta, 2025.

[3] T. Lattimore. Minimax regret for bandit convex optimisation of ridge functions. arXiv preprint arXiv:2106.00444, 2021.

[4] T. Lattimore. Improved regret for zeroth-order adversarial bandit convex optimisation. Mathematical Statistics and Learning, 2(3/4):311–334, 2020.

[5] S. Bubeck, O. Dekel, T. Koren, and Y. Peres. Bandit convex optimization: $\sqrt { T }$ regret in one dimension. In COLT, 2015.

[6] S. Bubeck and R. Eldan. Exploratory distributions for convex functions. Mathematical Statistics and Learning, 1(1):73–100, 2018.

[7] D. Russo and B. Van Roy. An information-theoretic analysis of Thompson sampling. Journal of Machine Learning Research, 17(1):2442–2471, 2016.

[8] N. Alon. Perturbed identity matrices have high rank: proof and applications. Combinatorics, Probability and Computing, 18(1–2):3–15, 2009.

[9] N. Alon, T. Lee, A. Shraibman, and S. Vempala. The approximate rank of a matrix and its algorithmic applications. In Proceedings of the 45th ACM Symposium on Theory of Computing (STOC), pages 675–684, 2013.

[10] N. Rajaraman, Y. Han, J. Jiao, and K. Ramchandran. Statistical complexity and optimal algorithms for nonlinear ridge bandits. Annals of Statistics, 52(6):2557–2582, 2024.

[11] N. Rajaraman and Y. Han. Interactive learning of single-index models via stochastic gradient descent. In International Conference on Learning Representations (ICLR), 2026. arXiv:2602.17876.

[12] N. Rajaraman and Y. Han. The price of hidden curvature: improved lower bounds for bandit convex optimization. arXiv:2607.18652 (v3), 2026.

[13] Y. Kang, M. Liu, B. Yi, J. Lyu, Z. Zhang, D. Zhou, and Y. Li. Single index bandits: generalized linear contextual bandits with unknown reward functions. In International Conference on Learning Representations (ICLR), 2026. arXiv:2506.12751.

[14] O. Kallenberg. Foundations of Modern Probability. Springer, 1997. Lemma 2.22.

[15] K. Kuratowski and C. Ryll-Nardzewski. A general theorem on selectors. Bull. Acad. Polon. Sci., 13:397–403, 1965.

## A Proofs for Sections 2 and 4

Proof of Lemma 2.1. Let $w _ { i } = \xi ( \mathscr { F } _ { i } )$ , discard pieces with $w _ { i } = 0$ , let $\xi _ { i }$ be the conditional law on ${ \mathcal { F } } _ { i }$ , and $J _ { i } = \mathbb { E } _ { \xi _ { i } \otimes \xi _ { i } } [ ( f ( x _ { g } ) - \bar { f } ( x _ { g } ) ) ^ { 2 } ]$ . Taking expectations of the hypothesis over k independent draws from $\xi _ { i }$ gives $\begin{array} { r } { \operatorname* { s u p } _ { f \in \mathcal { F } _ { i } } \bigl ( \bar { f } ( x _ { f } ) - f ^ { \star } \bigr ) \leq \alpha + \sqrt { \beta _ { 0 } k ( k - 1 ) J _ { i } } } \end{array}$ . Hence

$$
\begin{array} { r l } & { \displaystyle \Delta ( \pi _ { \mathrm { T S } } ^ { \xi } , \xi ) = \sum _ { i } w _ { i } \mathbb { E } _ { \xi _ { i } } [ \bar { f } ( x _ { f } ) - f ^ { \star } ] \leq \alpha + \sqrt { \beta _ { 0 } k ( k - 1 ) } \sum _ { i } w _ { i } \sqrt { J _ { i } } } \\ & { \quad \quad \quad \leq \alpha + \sqrt { \beta _ { 0 } k ( k - 1 ) m \sum _ { i } w _ { i } ^ { 2 } J _ { i } } \leq \alpha + \sqrt { \beta _ { 0 } k ( k - 1 ) m I ( \pi _ { \mathrm { T S } } ^ { \xi } , \xi ) } , } \end{array}
$$

by Cauchy–Schwarz and because $\begin{array} { r } { I ( \pi _ { \mathrm { T S } } ^ { \xi } , \xi ) = \sum _ { i , j } w _ { i } w _ { j } J _ { i j } \geq \sum _ { i } w _ { i } ^ { 2 } J _ { i } } \end{array}$ with $J _ { i j } = \mathbb { E } _ { \xi _ { i } \otimes \xi _ { j } } [ ( f ( x _ { g } ) -$ $\bar { f } ( x _ { g } ) ) ^ { 2 } ] \ge 0$ □

Proof of Lemma $4 . 1 .$ If $f$ is constant, any unit θ and the constant link work. Otherwise normalise the ridge direction and let u be the link on $I = \{ \langle x , \theta \rangle : x \in K \} = [ a , b ]$ . For $t \in ( a , b )$ there is $z \in$ int K with $\langle z , \theta \rangle = t$ and a short segment through z in direction θ inside $K .$ , along which $f$ is 1-Lipschitz, so u is locally 1-Lipschitz on $( a , b )$ and, by continuity of convex functions, 1-Lipschitz on I. Define $\ell ( t ) = u ( a ) + a - t$ for $t < a , \ell = u$ on $I ,$ and $\ell ( t ) = u ( b ) + t - b$ for $t > b$ . The slopes −1 on the left and +1 on the right bracket every secant slope of $u ,$ so ℓ is convex and 1-Lipschitz; outside I it exceeds the endpoint values, so min<sub>R</sub> $\ell = \operatorname* { m i n } _ { I } u = f ^ { \star }$ , attained at the projections of the minimisers of $f .$ The last claim is the 1-Lipschitz property of ℓ. □

Proof of Lemma $4 . 2 .$ Uninformativeness and the comparison condition give $f ( x _ { g } ) > \bar { f } ( x _ { g } ) - \delta \geq$ $\bar { f } ( x _ { f } ) - 2 \delta = f ^ { \star } + r _ { f } - 2 \delta \geq f ^ { \star } + \varepsilon / 2 - 2 \delta $ with 1-Lipschitzness of $\ell _ { f }$ this is (i), and $\varepsilon / 2 - 2 \delta \geq 5 \varepsilon / 1 2$ because $c \geq 1 2$ . For (ii) take g with $t _ { g } \mathrm { ~ > ~ } s$ and let $h$ attain $t _ { h } ~ = ~ b ~ ( \mathrm { s o } ~ h ~ \ne ~ f$ and $b > s )$ With $\lambda = ( b - t _ { q } ) / ( b - s ) \in [ 0 , 1 )$ , convexity of $\ell _ { f }$ gives $f ( x _ { g } ) \leq \lambda f ^ { \star } + ( 1 - \lambda ) f ( x _ { h } )$ . Using $f ^ { \star } \leq \bar { f } ( x _ { g } ) + \delta - \varepsilon / 2$ and $f ( x _ { h } ) < \bar { f } ( x _ { h } ) + \delta \leq \bar { f } ( x _ { g } ) + 2 \delta$

$$
f ( x _ { g } ) < \bar { f } ( x _ { g } ) + 2 \delta - \lambda ( \varepsilon / 2 + \delta ) ,
$$

and combining with $f ( x _ { g } ) > \bar { f } ( x _ { g } ) - \delta ~ \mathrm { y i e l d s } ~ \lambda < \eta$ . The left side is symmetric.

## B Proofs for Sections 5 and 6

Proof of Lemma 5.1. For symmetric M the second claim is $[ 8 ,$ , Lemma 2.2], which Alon attributes to earlier sources; the trace–rank inequality for arbitrary real matrices also appears as [7, Fact 10], and we include the short argument. Let Π be the orthogonal projection onto the column space of $M$ , of rank $q = \mathrm { r a n k } M$ . Then $\lvert \mathrm { t r } M \rvert = \lvert \mathrm { t r } ( \Pi M ) \rvert = \lvert \langle \Pi , M \rangle _ { F } \rvert \le \lVert \Pi \rVert _ { F } \lVert M \rVert _ { F } = \sqrt { q } \lVert M \rVert _ { F }$ . Inserting trM = N and $\| M \| _ { F } ^ { 2 } \le N + \dot { N } ( N - 1 ) t ^ { 2 }$ gives the second claim. For the third, $( Q + 1 ) - Q ( 1$ $t ^ { 2 } ) / ( 1 - Q t ^ { 2 } ) = ( 1 - Q ^ { 2 } t ^ { 2 } ) / ( 1 - Q t ^ { 2 } ) > 0$ when $t < 1 / Q$ , so $N < Q + 1$ □

Proof of Lemma 5.3. Suppose rank $B \geq 2 r$ and take a non-singular $2 r \times 2 r$ submatrix $B _ { 0 }$ of $B ;$ ; let $A _ { 0 }$ be the corresponding submatrix of A and $E = B _ { 0 } - A _ { 0 } .$ so rank $A _ { 0 } \leq r$ and $\| E \| _ { F } ^ { 2 } \le 4 r ^ { 2 } \gamma ^ { 2 }$ . Let $\sigma _ { 1 } \geq . . . \geq \sigma _ { 2 r } > 0$ be the singular values of $B _ { 0 }$ . Since dim ker $A _ { 0 } \geq r$ , let Π be the orthogonal projection onto an r-dimensional subspace of ker $A _ { 0 } ;$ then $B _ { 0 } \Pi = E \Pi$ , so $\| B _ { 0 } \Pi \| _ { F } ^ { 2 } \leq \| E \| _ { F } ^ { 2 }$ . In an orthonormal basis of right singular vectors of $\begin{array} { r } { B _ { 0 } , \| B _ { 0 } \Pi \| _ { F } ^ { 2 } = \sum _ { k } \sigma _ { k } ^ { 2 } w _ { k } } \end{array}$ with $0 \leq w _ { k } \leq 1$ and $\textstyle \sum _ { k } w _ { k } = r$ , which is at least the sum of the $r$ smallest $\sigma _ { k } ^ { 2 }$ . Hence

$$
\sum _ { k = r + 1 } ^ { 2 r } \sigma _ { k } ^ { 2 } \le 4 r ^ { 2 } \gamma ^ { 2 } , \qquad \sum _ { k = 1 } ^ { r } \sigma _ { k } ^ { 2 } \le \| B _ { 0 } \| _ { F } ^ { 2 } \le 4 r ^ { 2 } - 1 ,
$$

the latter because a non-singular $2 r \times 2 r ~ 0 { - } 1$ matrix has at least one zero entry. By the arithmetic– geometric mean inequality applied to each group of r squared singular values,

$$
| \operatorname* { d e t } B _ { 0 } | = \prod _ { k = 1 } ^ { 2 r } \sigma _ { k } \leq \Big ( \frac { 4 r ^ { 2 } - 1 } { r } \cdot \frac { 4 r ^ { 2 } \gamma ^ { 2 } } { r } \Big ) ^ { r / 2 } = ( 4 r \gamma ) ^ { r } \Big ( 1 - \frac { 1 } { 4 r ^ { 2 } } \Big ) ^ { r / 2 } < 1 \qquad \mathrm { f o r } \ \gamma \leq \frac { 1 } { 4 r } ,
$$

contradicting | det $| B _ { 0 } | \ge 1$ for a non-singular integer matrix.

Proof of Lemma 6.1. As long as the remaining set has more than G elements it is not uninformative, so it contains an ordered pair $( f , g )$ with $| f ( x _ { g } ) - { \bar { f } } ( x _ { g } ) | \geq \delta ;$ delete $f$ (only the first endpoint). The hypotheses of Definition 3.2 are per-element and pairwise, so they survive deletion, and before the $( j + 1 ) \mathrm { - s t }$ deletion $( 0 \leq j \leq q - 1 )$ at least $G + q - j \geq G + 1$ elements remain. The $q$ recorded ordered pairs have distinct first endpoints, hence are distinct, and each contributes at least $\delta ^ { 2 }$ .

## C Proofs for Section 7

Proof of Lemma 7.1. By the randomisation lemma for kernels into Borel spaces [14, Lemma 2.22] there is a jointly measurable $a ( f , u )$ with $X = a ( f , U )$ for U uniform on [0, 1] independent of $f .$ Let $a _ { 0 }$ be a fixed measurable selection, $B = \{ ( f , u ) : f ( a ( f , u ) ) > f ^ { \star } \}$ , and $\tilde { a } = a$ of $B , \tilde { a } = a _ { 0 }$ on $B ;$ then a˜ is jointly measurable, $f \mapsto \tilde { a } ( f , u )$ is a measurable selection for every $u ,$ and $( f , \tilde { a } ( f , U ) )$ has the same law as $( f , X )$ . Let $\pi _ { u }$ be the law of $\tilde { a } ( f , u )$ . Both $\Delta ( \cdot , \xi )$ and $I ( \cdot , \xi )$ are linear in the policy, so $\begin{array} { r } { \Delta ( \mathcal { L } ( X ) , \xi ) = \int \Delta ( \pi _ { u } , \xi ) d u \leq \alpha + \int \sqrt { \beta I ( \pi _ { u } , \xi ) } d u \leq \alpha + \sqrt { \beta \int I ( \pi _ { u } , \xi ) } } \end{array}$ du by concavity of the square root. □

Proof of Lemma 7.2. For $( X , h ) \sim \pi \otimes \nu$ one has $\bar { h } ( X ) = \mathbb { E } [ h ( X ) \mid X ]$ , so $I ( \pi , \nu ) ^ { 1 / 2 } = \| h ( X ) -$ $\mathbb { E } [ h ( X ) \mid X ] \| _ { L ^ { 2 } \cdot \mathrm { \Omega } } ( \mathrm { a } )$ : realise $X \sim \pi$ independently of the coupled pair $( f , g )$ and let $Q W = W - \mathbb { E } [ W \ |$ $X ]$ , an $L ^ { 2 }$ contraction; then $\sqrt { I ( \pi , \mathcal { L } ( f ) ) } = \| Q f ( X ) \| _ { L ^ { 2 } } , \sqrt { I ( \pi , \mathcal { L } ( g ) ) } = \| Q g ( X ) \| _ { L ^ { 2 } }$ , and the two difer by at most $\| Q ( g ( X ) - f ( X ) ) \| _ { L ^ { 2 } } \leq \| g ( X ) - f ( X ) \| _ { L ^ { 2 } } \leq \rho . \mathrm { ( b ) }$ : realise $h \sim \nu$ independently of the coupled pair $( X , Z )$ and use the single centring operator $Q W = W - \mathbb { E } [ W \mid X , Z ] .$ ; independence gives $Q h ( X ) = h ( X ) - \bar { h } ( X )$ and $Q h ( Z ) = h ( Z ) - \bar { h } ( Z )$ , so $\sqrt { I ( \mathcal { L } ( Z ) , \nu ) }$ and $\sqrt { I ( \mathcal { L } ( X ) , \nu ) }$ difer by at most $\| Q ( h ( Z ) - h ( X ) ) \| _ { L ^ { 2 } } \leq \| h ( Z ) - h ( X ) \| _ { L ^ { 2 } } \leq \rho$ by the Lipschitz bound. (c): let $g = \operatorname* { m a x } ( f , f ( X ) )$ truncation at a level in $[ 0 , 1 ]$ preserves convexity, range, Lipschitzness and the ridge direction, so $g \in { \mathcal { F } } _ { \mathrm { b l r } } ;$ X is an exact minimiser of $g , \| g - f \| _ { \infty } \leq \rho , { \bar { f } } \leq { \bar { g } }$ and $f ^ { \star } \geq g ^ { \star } - \rho$ . Since $( f , X )$ takes finitely many values, $( g , X )$ is a finitely-valued measurable kernel of exact minimisers (distinct values of $( f , X )$ may share $g$ but not $X$ , which is why a kernel rather than a selection is needed), and Lemma 7.1 gives $\Delta ( \mathcal { L } ( X ) , \mathcal { L } ( f ) ) \leq \Delta ( \mathcal { L } ( X ) , \mathcal { L } ( g ) ) + \rho \leq \alpha + \sqrt { \beta I ( \mathcal { L } ( X ) , \mathcal { L } ( g ) ) } + \rho$ . Finally, since $I ( \pi , \nu )$ depends only on the marginal law of the policy and the marginal law of the random function, part (a) may be applied on a product coupling in which an independent $X ^ { \prime } \sim { \mathcal { L } } ( X )$ is used together with the coupled pair $( f , g ) , \| g - f \| _ { \infty } \leq \rho ;$ this gives $\sqrt { I ( \mathcal { L } ( X ) , \mathcal { L } ( g ) ) } \leq \sqrt { I ( \mathcal { L } ( X ) , \mathcal { L } ( f ) ) } + \rho$ and finishes the proof. □

Proof of Lemma 7.3. A unit vector θ is a ridge direction of f if and only ${ \mathrm { i f ~ } } | f ( x ) - f ( y ) | \leq | \langle \theta , x - y \rangle |$ for all $x , y \in K$ (the forward direction is Lemma 4.1, the backward direction yields a profile that is constant on fibres, convex and 1-Lipschitz). Let $\Phi ( f )$ be the set of such θ. Profiles extended as in Lemma 4.1 satisfy $\ell ( 0 ) = f ( 0 ) \in [ 0 , 1 ]$ and $\operatorname { L i p } ( \ell ) \leq 1 \quad$ , so they are uniformly bounded and equicontinuous on $[ - D , D ] ; { \mathrm { i f ~ } } f _ { j } \to f$ uniformly and $\theta _ { j } \to \theta$ , a uniformly convergent subsequence of profiles shows $f = \ell ( \langle \cdot , \theta \rangle )$ . Hence $\mathcal { F } _ { \mathrm { b l r } }$ is compact in $C ( K )$ (Arzel\`a–Ascoli) and the graph of Φ is closed in $\mathcal { F } _ { \mathrm { b l r } } \times S ^ { d - 1 }$ , so $\Phi$ has non-empty closed values. For an open $O \subseteq S ^ { d - 1 }$ write

$O = \cup _ { i } C _ { j }$ with $C _ { j }$ compact; then $\{ f : \Phi ( f ) \cap O \neq \emptyset \} = \bigcup _ { j } \operatorname { p r o j } ( \operatorname { G r a p h } ( \Phi ) \cap ( { \mathcal { F } } _ { \mathrm { b l r } } \times C _ { j } ) )$ is a countable union of compact sets, hence Borel, so Φ is weakly measurable and the Kuratowski– Ryll-Nardzewski selection theorem [15] gives a Borel selection $\theta _ { f } \in \Phi ( f )$ . The map L satisfies $| L ( f , \theta , t ) - L ( g , \psi , s ) | \leq \| f - g \| _ { \infty } + \bar { D } \| | \theta - \psi \| + | t - s |$ , hence is jointly continuous, and for $\theta \in \Phi ( f )$ it coincides with the profile of f on the projection interval and with the ∓1-slope extension of Lemma 4.1 outside it. □

Proof of Lemma 7.4. (i) Averages of convex ridge functions with a common direction are convex ridge functions with that direction; range and Lipschitz bounds are preserved; and if $\textstyle g _ { \nu } = \int g d \nu$ then $\begin{array} { r } { g _ { \nu } ( x ) \le \int g ^ { \star } d \nu + 2 \rho \le g _ { \nu } ^ { \star } + 2 \rho } \end{array}$ since the minimum of an average is at least the average of the minima. (ii) Let $\theta _ { f }$ and L be as in Lemma 7.3 and let $\kappa ( f )$ be the first pair in a fixed enumeration of $C _ { K } \times C _ { S }$ with $\| x _ { \kappa } - x _ { f } \| \le \rho$ and $\lVert \theta _ { \kappa } - \theta _ { f } \rVert \leq \rho / ( 2 D )$ ; since $x _ { f }$ is A-measurable and $\theta _ { f }$ is Borel, κ is A-measurable. Define

$$
g _ { f } ( x ) = L ( f , \theta _ { \kappa } , \langle x , \theta _ { \kappa } \rangle ) = \operatorname* { m i n } _ { y \in K } \big [ f ( y ) + | \langle \theta _ { \kappa } , x - y \rangle | \big ] .
$$

The profile $t \mapsto L ( f , \theta _ { \kappa } , t )$ is convex (the minimand is jointly convex in $( t , y )$ and partial minimisation over the convex set K preserves convexity) and 1-Lipschitz (each y contributes a 1-Lipschitz function of $t )$ , so $g _ { f }$ is a convex ridge function in direction $\theta _ { \kappa }$ with $\mathrm { L i p } ( g _ { f } ) \le 1$ . Taking $y = x$ gives $g _ { f } \leq f \leq 1$ , and $f \geq f ^ { \star }$ gives $g _ { f } \geq f ^ { \star } \geq 0 ;$ hence $g _ { f } \in \mathcal { F } _ { \mathrm { b l r } }$ with no rescaling. By Lemma 4.1, for all $x , y \in K , f ( x ) - f ( y ) \leq | \langle \theta _ { f } , x - y \rangle | \leq | \langle \theta _ { \kappa } , x - y \rangle | + D | | \theta _ { \kappa } - \theta _ { f } | | \leq | \langle \theta _ { \kappa } , x - y \rangle | + \rho / 2$ (using $\| x - y \| \leq D )$ ; minimising over y gives $f ( x ) - \rho / 2 \leq g _ { f } ( x ) \leq f ( x ) , { \mathrm { i . e . ~ } } \| g _ { f } - f \| _ { \infty } \leq \rho / 2$ . At the selected minimiser, $f ^ { \star } \leq g _ { f } ( x _ { f } ) \leq f ( x _ { f } ) = f ^ { \star }$ , so $x _ { f }$ minimises $g _ { f }$ and $g _ { f } ^ { \star } = f ^ { \star } ;$ ; by 1-Lipschitzness $g _ { f } ( x _ { \kappa } ) - g _ { f } ^ { \star } \leq \| x _ { \kappa } - x _ { f } \| \leq \rho \leq 2 \rho , \operatorname { s o } g _ { f } \in \mathcal { F } _ { \kappa ( f ) } ^ { \mathrm { v a l } }$ . Finally $f \mapsto g _ { f }$ is measurable as a $( C ( K ) , B )$ -valued map and $( f , x ) \mapsto g _ { f } ( x )$ is jointly measurable, by the joint continuity of L and the measurability of $\kappa .$ □

Proof of Theorem 7.5. We bound the one-step regret under an arbitrary posterior and then sum. Let $f \sim \xi$ (a posterior at some round), $X ^ { \star } = x _ { f }$ , and let X be an independent copy of $X ^ { \star }$ , so ${ \mathcal { L } } ( X ) = \pi _ { \mathrm { T S } }$ . Let $\kappa = \kappa ( f ) , g = g _ { f } , z = x _ { \kappa }$ be as in Lemma 7.4, and

$$
H _ { \kappa } = \mathbb { E } [ f \mid \kappa ] , \qquad G _ { \kappa } = \mathbb { E } [ g \mid \kappa ] , \qquad \nu _ { G } = \mathcal { L } ( G _ { \kappa } ) , \qquad \pi _ { z } = \mathcal { L } ( z ) .
$$

Both $H _ { \kappa }$ and $G _ { \kappa }$ take finitely many values (one per label). $G _ { \kappa }$ is a finitely-valued $( \mathcal { F } _ { \mathrm { b l r } } , \mathcal { A } )$ -valued random element; $H _ { \kappa }$ is viewed only as a Borel $C ( K )$ -valued random element with values in $\mathcal { F } _ { \mathrm { b l } }$ By Lemma $7 . 4 ( \mathrm { i } ) , G _ { \kappa } \in \mathcal { F } _ { \mathrm { b l r } }$ is a ridge function in direction $\theta _ { \kappa }$ and is 2ρ-near-minimal at $z ; H _ { \kappa }$ is merely a convex 1-Lipschitz function $K  [ 0 , 1 ]$ (an average of ridge functions with nearby but diferent directions is in general not a ridge function), and $\| G _ { \kappa } - H _ { \kappa } \| _ { \infty } \leq \rho / 2$ . In this proof the information-ratio hypothesis is applied, via Lemmas 7.1 and $7 . 2 ( \mathrm { c } )$ , to the finitely-valued flattened proxy max $\{ G _ { \kappa } , G _ { \kappa } ( z ) \}$ , never to $H _ { \kappa } ; H _ { \kappa }$ enters only through the continuity statements of Lemma $7 . 2 ( \mathrm { a } ) , ( \mathrm { b } )$ , which do not require the ridge structure.

First, $\begin{array} { r } { \mathbb { E } [ f ^ { \star } \mid \kappa ] = \mathbb { E } [ f ( X ^ { \star } ) \mid \kappa ] \ge \mathbb { E } [ g ( X ^ { \star } ) \mid \kappa ] \ge G _ { \kappa } ( z ) - \rho \ge G _ { \kappa } ^ { \star } - \rho \mathrm { ~ ( u s i n g ~ } g \le f \mathrm { , ~ } \| X ^ { \star } - z \| \le \rho \mathrm { , ~ } \| x \| _ { \infty } \le \| z \| _ { \infty } \mathrm { , ~ } \rho \| _ { \infty } \mathrm { . ~ } } \end{array}$ and Lipschitzness), and $\| \mathbb { E } f - \mathbb { E } g \| _ { \infty } \leq \rho / 2 ;$ hence

$$
\begin{array} { r } { \Delta ( \pi _ { \mathrm { T S } } , \xi ) = \mathbb { E } [ \bar { f } ( X ) - f ^ { \star } ] \le \mathbb { E } [ \bar { g } ( X ) - G _ { \kappa } ^ { \star } ] + \frac { 3 } { 2 } \rho \le \mathbb { E } [ \bar { g } ( z ) - G _ { \kappa } ^ { \star } ] + \frac { 5 } { 2 } \rho = \Delta ( \pi _ { z } , \nu _ { G } ) + \frac { 5 } { 2 } \rho . } \end{array}
$$

By Lemma $7 . 2 ( \mathrm { c } )$ applied to the finitely-valued pair $( G _ { \kappa } , z ) , \ \Delta ( \pi _ { z } , \nu _ { G } ) \le \alpha + \sqrt { \beta I ( \pi _ { z } , \nu _ { G } ) } +$ $2 \rho ( 1 + { \sqrt { \beta } } )$ . The information terms depend only on the product of the marginal laws, so the continuity statements may be applied on a product space: let $F _ { 1 } , F _ { 2 }$ be independent draws from the current posterior, build $\left( G _ { 1 } , H _ { 1 } \right) = \left( G _ { \kappa ( F _ { 1 } ) } , H _ { \kappa ( F _ { 1 } ) } \right)$ from $F _ { 1 }$ and $\left( Z _ { 2 } , A _ { 2 } \right) = \left( x _ { \kappa ( F _ { 2 } ) } , x _ { F _ { 2 } } \right)$ from $F _ { 2 }$ . Lemma $7 . 2 ( \mathrm { a } )$ with the coupling $( G _ { 1 } , \dot { H _ { 1 } } ) , \ : \| G _ { 1 } - \dot { H _ { 1 } } \| _ { \infty } \leq \rho / 2$ , independent of $Z _ { 2 } \sim \pi _ { z } ,$ gives $\sqrt { I ( \pi _ { z } , \nu _ { G } ) } \leq \sqrt { I ( \pi _ { z } , \mathcal { L } ( H _ { \kappa } ) ) } + \rho / 2 ;$ ; Lemma $7 . 2 ( \mathrm { b } )$ with the coupling $( Z _ { 2 } , A _ { 2 } ) , \ \lVert Z _ { 2 } - A _ { 2 } \rVert \ \leq \rho .$ independent of $H _ { 1 } \sim { \mathcal { L } } ( H _ { \kappa } )$ , gives $\sqrt { I ( \pi _ { z } , \mathcal { L } ( H _ { \kappa } ) ) } \leq \sqrt { I ( \pi _ { \mathrm { T S } } , \mathcal { L } ( H _ { \kappa } ) ) } + \rho$ . Together $\sqrt { I ( \pi _ { z } , \nu _ { G } ) } \leq$ $\begin{array} { r } { \sqrt { I ( \pi _ { \mathrm { T S } } , \mathcal { L } ( H _ { \kappa } ) ) } + \frac { 3 } { 2 } \rho . } \end{array}$ Altogether

$$
\begin{array} { r } { \Delta ( \pi _ { \mathrm { T S } } , \xi ) \leq \alpha + \sqrt { \beta I ( \pi _ { \mathrm { T S } } , \mathcal { L } ( H _ { \kappa } ) ) } + \rho \big ( \frac 9 2 + \frac 7 2 \sqrt { \beta } \big ) \leq \alpha + \sqrt { \beta I ( \pi _ { \mathrm { T S } } , \mathcal { L } ( H _ { \kappa } ) ) } + \rho ( 6 + 4 \sqrt { \beta } ) . } \end{array}
$$

For Bernoulli observations, $I ( \pi _ { \mathrm { T S } } , { \mathcal { L } } ( H _ { \kappa } ) ) = \mathbb { E } [ ( \mathbb { E } [ Y \mid X ] - \mathbb { E } [ Y \mid X , \kappa ] ) ^ { 2 } ] \leq { \frac { 1 } { 2 } } \mathbb { E } [ \mathrm { K L } ( P _ { Y \mid X , \kappa } \parallel P _ { Y \mid X } ) ] =$ $\frac { 1 } { 2 } \mathsf { I } ( \kappa ; X , Y )$ by Pinsker’s inequality. The label $\kappa = \kappa ( f )$ is a fixed finite-valued function of the environment (it depends on the fixed nets, the fixed direction map of Lemma 7.3 and the fixed selection, not on the round), so summing over rounds and using the chain rule for mutual information and Cauchy–Schwarz,

$$
\begin{array} { r } { \mathrm { { B R e g } } _ { n } ( \mathrm { T S } , \xi ) \leq n \alpha + n \rho ( 6 + 4 \sqrt { \beta } ) + \sqrt { \frac { 1 } { 2 } \beta n \mathbb { E } \sum _ { t } \mathfrak { l } _ { t - 1 } ( \kappa ; X _ { t } , Y _ { t } ) } \leq n \alpha + n \rho ( 6 + 4 \sqrt { \beta } ) + \sqrt { \frac { 1 } { 2 } \beta n \log N _ { \rho - 1 } } } \end{array}
$$

Proof of Theorem 3.1. For $n = 1$ the regret is at most $1 \leq 7$ . Let $n \geq 2 , \alpha = \rho = 1 / n , L = \log ( e +$ nd max $\{ 1 , D \} )$ , and $h = d { + } 1$ . From Theorem $3 . 4 , k = 3 0 7 2 h ^ { 4 }$ and $m _ { 1 / n } \leq 4 L$ , so ${ \sqrt { \beta } } \leq { \sqrt { 1 2 \cdot 4 L } } k =$ $4 { \sqrt { 3 } } \cdot 3 0 7 2 h ^ { 4 } { \sqrt { L } } = 1 2 , 2 8 8 { \sqrt { 3 } } h ^ { 4 } { \sqrt { L } }$ . From Lemma 7.4, log $N _ { 1 / n } \leq d \log ( 1 + 2 n D ) + d \log ( 1 + 8 n D ) \leq$ 8dL. Theorem 7.5 gives $\mathrm { B R e g } _ { n } \leq 1 + 6 + 4 { \sqrt { \beta } } + { \sqrt { \beta } } { \sqrt { 4 n d L } } \leq 7 + { \sqrt { \beta } } \left( 4 + 2 { \sqrt { n d L } } \right) \leq 7 + 6 { \sqrt { \beta } } { \sqrt { n d L } }$ because ${ \sqrt { n d L } } \geq 1 , { \mathrm { i . e . ~ B R e g } } _ { n } \leq 7 + 7 3 , 7 2 8 { \sqrt { 3 } } h ^ { 4 } d ^ { 1 / 2 } { \sqrt { n } } L$ □

## D Proofs for Section 8

Proof of Proposition 3.5. Let $d \geq 2 , c \geq 1 2 , \tau = 1 / ( 4 c ( d + 1 ) )$ . Take the standard d-simplex with vertices $v _ { 0 } , \ldots , v _ { d }$ and barycentric coordinates $\lambda _ { 0 } , \ldots , \lambda _ { d }$ (translated so that the average of the points below is 0). For each ordered pair $i \neq j$ let $x _ { i j } = ( 1 - \tau ) v _ { i } + \tau v _ { j }$ , and let $K = \mathrm { c o n v } \{ x _ { i j } \}$ (full-dimensional, containing $0 , D \leq { \sqrt { 2 } } )$ . Let $u _ { i j } ( x ) = 1 - \lambda _ { i } ( x ) + \tau \lambda _ { j } ( x )$ , an afine function, and set $v = 1 / 4 , \varepsilon = \tau ^ { 2 } / ( 4 d ) , \bar { f } \equiv v , s ^ { u } = \tau + \tau ^ { 2 }$ , and

$$
f _ { i j } ( x ) = v - \varepsilon + \varepsilon \operatorname* { m a x } \Big \{ \frac { s ^ { u } - u _ { i j } ( x ) } { \tau ^ { 2 } } , ~ \frac { u _ { i j } ( x ) - s ^ { u } } { 1 - 2 \tau ^ { 2 } } \Big \} , \qquad x _ { f _ { i j } } = x _ { i j } .
$$

Each $f _ { i j }$ is the maximum of two afine functions, hence a convex ridge function, minimised exactly where $u _ { i j } = s ^ { u }$ , in particular at $x _ { i j } .$ , with minimum $v - \varepsilon$ . Evaluating $u _ { i j }$ at the $d ( d + 1 )$ points gives exactly the values ${ \tau } + { \tau } ^ { 2 } \ ( \mathrm { a t \ } x _ { i j } ) , { \tau }$ (the d − 1 points $x _ { i l } ) , 1 - \tau ^ { 2 } ( x _ { j i } ) , 1 + \tau - \tau ^ { 2 } ( x _ { j l } ) , 1 - \tau ( x _ { k i } )$ $1 + \tau ^ { 2 } \left( x _ { k j } \right)$ and 1 (the remaining $( d - 1 ) ( d - 2 ) \ \mathrm { p o i n t s } )$ , so on K the values of $f _ { i j }$ lie in $[ v - \varepsilon , v ]$ the points $x _ { i l }$ have $f _ { i j } = v$ exactly, and all other cross values satisfy $| f _ { i j } ( x _ { k l } ) - v | \le \varepsilon E _ { \tau }$ with $E _ { \tau } = ( 2 \tau - \tau ^ { 2 } ) / ( 1 - 2 \tau ^ { 2 } ) < 3 \tau$ . Since $\delta = \varepsilon / ( c ( d + 1 ) ) = 4 \tau \varepsilon$ , every ordered pair is uninformative; the gaps are $r _ { f } = \varepsilon$ and the comparison values coincide. Lipschitzness: $\| \nabla u _ { i j } \| \le ( 1 + \tau ) \sqrt { d }$ in the standard simplex coordinates, so $\mathrm { L i p } ( f _ { i j } ) \leq \varepsilon ( 1 + \tau ) \sqrt { d } / \tau ^ { 2 } = ( 1 + \tau ) / ( 4 \sqrt { d } ) < 1$ . The functions are pairwise distinct because only $f _ { i j }$ attains $v - \varepsilon { \mathrm { ~ a t ~ } } x _ { i j }$ . Along its own direction each row sees the $d - 1$ points $x _ { i l }$ at distance $\tau ^ { 2 }$ in u<sub>ij</sub>-coordinates (the near band; the unit-direction distances of Lemma 4.2 are these values divided by $\| \nabla u _ { i j } \| )$ and the other $d ^ { 2 }$ points at $u _ { i j }$ -distance about 1 (the far band). Finally $D / \varepsilon \le \sqrt { 2 } \cdot 4 d / \tau ^ { 2 } = 6 4 \sqrt { 2 } c ^ { 2 } d ( d + 1 ) ^ { 2 }  { \mathrm { \Omega } }$ ; for every $0 < \varepsilon ^ { \prime } \leq \varepsilon$ keep $K$ , the selected points and the ridge directions fixed and replace each function by $f _ { i j } ^ { \prime } = v + ( \varepsilon ^ { \prime } / \varepsilon ) ( f _ { i j } - v )$ ， with comparison function ${ \bar { f } } ^ { \prime } \equiv v \colon$ the diagonal gaps and the cross errors are multiplied by $\varepsilon ^ { \prime } / \varepsilon$ while the Lipschitz constants do not increase, so the new configuration is uninformative at scale $\varepsilon ^ { \prime } \left( \mathrm { w i t h } \ \delta ^ { \prime } = \varepsilon ^ { \prime } / ( c ( d + 1 ) ) \right)$ and every larger ratio $D / \varepsilon ^ { \prime }$ is attained. The stated parameter cost for $c = 9 6 ( d + 1 ) $ follows by substituting $\tau = 1 / ( 3 8 4 ( d + 1 ) ^ { 2 } )$ and $\varepsilon = \tau ^ { 2 } / ( 4 d )$ ; the upper bound is Theorem 3.3. For $d = 1$ take $K = [ 0 , 1 ] , \bar { f } \equiv 1 / 4 , f _ { 1 } = 1 / 4 - \varepsilon + \varepsilon x , f _ { 2 } = 1 / 4 - \varepsilon + \varepsilon ( 1 - x )$ with minimisers $0 , 1$ and $0 < \varepsilon \le 1 / 4$ , which requires $D / \varepsilon \geq 4$ . The bound $| C | \le 2$ in dimension one follows from Lemma $4 . 2 ( \mathrm { i i } )$ : if $x _ { 1 } < x _ { 2 } < x _ { 3 }$ are three minimisers of an uninformative configuration, the row of $x _ { 1 }$ sees $x _ { 2 }$ in its far band, $x _ { 2 } \geq x _ { 3 } - \eta ( x _ { 3 } - x _ { 1 } )$ , and the row of $x _ { 3 }$ sees $x _ { 2 }$ in its far band, $x _ { 2 } \leq x _ { 1 } + \eta ( x _ { 3 } - x _ { 1 } )$ ; together $( 1 - 2 \eta ) ( x _ { 3 } - x _ { 1 } ) \leq 0$ , contradicting $\eta < 1 / 2$ . (For $D / \varepsilon \leq 5 / 1 2$ not even two points fit, since Lemma 4.2(i) forces a distance larger than $5 \varepsilon / 1 2 . )$ □

Proof of Proposition $3 . 6 .$ Take the regular tetrahedron $v _ { 0 } = { \textstyle \frac { 1 } { 2 } } ( 1 , 1 , 1 ) , v _ { 1 } = { \textstyle \frac { 1 } { 2 } } ( 1 , - 1 , - 1 ) , v _ { 2 } =$ $\begin{array} { r } { \frac 1 2 ( - 1 , 1 , - 1 ) , v _ { 3 } = \frac 1 2 ( - 1 , - 1 , 1 ) , \mathrm { ~ s o ~ } \lambda _ { i } ( x ) = \frac 1 4 + \langle v _ { i } , x \rangle } \end{array}$ , and use the configuration of Proposition 3.5 with $K = P : = \mathrm { c o n v } \{ x _ { i j } \}$ (a truncated tetrahedron; the twelve points are its vertices). The inscribed ball of the tetrahedron $S = \mathrm { c o n v } \{ v _ { i } \}$ is $B _ { r }$ with $r = 1 / ( 2 \sqrt { 3 } )$ , and on $B _ { r }$ one has $0 \leq \lambda _ { i } \leq 1 / 2$ , so $B _ { r } \subset P$ . Fix a vertex $x _ { i j }$ and let $b _ { 0 } = ( 1 - 2 \tau ) / ( 1 - \tau )$ . The afine inequality $\lambda _ { i } + b _ { 0 } \lambda _ { j } \leq 1 - \tau$ is violated by $x _ { i j }$ , holds with equality at its three neighbours $x _ { i k } \ ( k \neq i , j )$ and $x _ { j i } .$ , and holds at all other vertices; since the cutting plane meets every edge from $x _ { i j }$ exactly at a neighbour, $P _ { - i j } : = \mathrm { c o n v } \{ x _ { k l } : ( k , l ) \neq ( i , j ) \} = P \cap \{ \lambda _ { i } + b _ { 0 } \lambda _ { j } \leq 1 - \tau \}$ . On $B _ { r } , \operatorname* { m a x } ( \lambda _ { i } + b _ { 0 } \lambda _ { j } ) =$ $\left( 1 + b _ { 0 } + { \sqrt { 1 + b _ { 0 } ^ { 2 } - 2 b _ { 0 } / 3 } } \right) / 4 \leq ( 2 + { \sqrt { 2 } } ) / 4 < 1 - \tau { \mathrm { ~ f o r ~ } } \tau \leq 1 / 1 9 2$ , which holds for all $c \geq 1 2 ;$ hence $B _ { r } \subset P _ { - i j }$ for all $i \neq j$ . Now let $\zeta > 0$ . Both $J _ { \zeta } ( C ) = P + B _ { \zeta }$ and $J _ { \zeta } ( C \backslash \{ f _ { i j } \} ) = P _ { - i j } + B _ { \zeta }$ contain $B _ { r + \zeta }$ and are contained in the enlarged tetrahedron $S _ { \zeta } = \{ x : \langle u _ { k } , x \rangle \leq r + \zeta , \ k = 0 , \ldots , 3 \}$ , where $u _ { k }$ are the unit outer normals of S, which satisfy $\textstyle \sum _ { k } u _ { k } = 0$ and $\begin{array} { r } { \sum _ { k } u _ { k } u _ { k } ^ { \top } = \frac { 4 } { 3 } I . } \end{array}$ If an ellipsoid $z + A B _ { 1 } \ ( A \succ 0$ symmetric) lies in $S _ { \zeta }$ then $\left. u _ { k } , z \right. + \left\| A u _ { k } \right\| \leq r + \zeta$ for each $k ;$ summing and using $\lvert | A u _ { k } \rvert | \geq u _ { k } ^ { \top }$ Au<sub>k</sub> gives ${ \begin{array} { l } { { \frac { 4 } { 3 } } \mathrm { t r } } A \leq 4 ( r + \zeta ) } \end{array} $ , so det $A \leq ( \operatorname { t r } A / 3 ) ^ { 3 } \leq ( r + \zeta ) ^ { 3 }$ . Hence the maximum-volume inscribed ellipsoid of both bodies is $B _ { r + \zeta }$ (equality in the arithmetic–geometric mean inequality forces $A = ( r + \zeta ) I$ and then $z = 0 )$ , and every single-vertex removal leaves it unchanged. □

## E A limit of max-norm rounding

The tolerance $1 / ( 4 r )$ in Lemma 5.3 has the order $1 / r$ . The following proposition shows that, for arbitrary 0-1 matrices, no tolerance of order $1 / \sqrt { r }$ can work; it is used only in the discussion of Section 9.

Proposition E.1 (Max-norm rounding cannot have a uniform $1 / \sqrt { r }$ tolerance). For every $r = 2 ^ { j } - 1$ with $j \geq 2$ there are $A , B \in \mathbb { R } ^ { 2 r \times 2 r }$ such that rank $A = r$ , B is a 0-1 matrix with rank $B = 2 r$ , and $\| A - B \| _ { \operatorname* { m a x } } = 2 / ( r + 1 )$ . Consequently, for every constant $a > 0$ there are infinitely many $r \ f o r$ which the implication “rank $A \leq r$ and $\| A - B \| _ { \operatorname* { m a x } } \leq a / \sqrt { r }$ for $\textit { a } 0 { - } 1$ matrix B imply rank $B \leq 2 r - 1 ^ { \ast }$ fails.

Proof. Let $s = r + 1 = 2 ^ { j }$ and let $S _ { s }$ be the Sylvester–Hadamard matrix of order $s ,$ defined by $S _ { 1 } = ( 1 )$ and $\begin{array} { r } { S _ { 2 m } = \left( \begin{array} { l l } { S _ { m } } & { S _ { m } } \\ { S _ { m } } & { - S _ { m } } \end{array} \right) } \end{array}$ ; it is symmetric with entries $\pm 1$ , its first row and column consist of ones, and $S _ { s } S _ { s } ^ { \top } = s I _ { s }$ . Write $\boldsymbol { S _ { s } } = \left( \mathbf { 1 } \mathbf { 1 } ^ { \intercal } \right)$ with $C$ a symmetric $r \times r$ matrix with entries $\pm 1$ Orthogonality of each of the rows $2 , \ldots , s$ to the first row gives $C \mathbf { 1 } = - \mathbf { 1 }$ , and orthogonality among the rows $2 , \ldots , s$ gives $J + C C ^ { \top } = s I _ { r }$ , where $J = \mathbf { 1 1 } ^ { \top }$ ; that is, $C C ^ { \top } = s I _ { r } - J$ . Let $W = ( J { - } C ) / 2$ a 0-1 matrix. Using $J C ^ { \top } = \mathbf { 1 } ( C \mathbf { 1 } ) ^ { \top } = - J$

$$
W \bigg ( - \frac { 2 } { s } C ^ { \top } \bigg ) = - \frac { 1 } { s } \big ( J C ^ { \top } - C C ^ { \top } \big ) = - \frac { 1 } { s } \big ( - J - s I _ { r } + J \big ) = I _ { r } ,
$$

so $W$ is non-singular with $\begin{array} { r } { W ^ { - 1 } = - \frac { 2 } { s } C ^ { \top } } \end{array}$ , whose entries are $\pm 2 / s ;$ hence $\| W ^ { - 1 } \| _ { \operatorname* { m a x } } = 2 / ( r + 1 )$ Now set

$$
A = { \binom { I _ { r } } { W ^ { - 1 } } } \quad W \atop I _ { r } \int = { \binom { I _ { r } } { W ^ { - 1 } } } \left( I _ { r } \quad W \right) , \qquad B = { \binom { I _ { r } } { 0 } } \quad W \atop I _ { r } \int .
$$

The factorisation shows rank $A \leq r$ , and the top-left block $I _ { r }$ shows rank $A \geq r$ . B is a 0-1 matrix, block upper triangular with identity diagonal blocks, so det $B = 1$ and rank $B = 2 r$ . Finally $A - B$ has the single non-zero block $W ^ { - 1 }$ , so $\| A - B \| _ { \operatorname* { m a x } } = \| W ^ { - 1 } \| _ { \operatorname* { m a x } } = 2 / ( r + 1 )$ . For the consequence, $2 / ( r + 1 ) \leq a / \sqrt { r }$ as soon as $2 { \sqrt { r } } / ( r + 1 ) \leq a$ , which holds for all large r since $2 \sqrt { r } / ( r + 1 ) \to 0$ □

The smallest instance is $r = 3$ , where the construction gives $W = J _ { 3 } - I _ { 3 }$ up to a row permutation, with $\| A - B \| _ { \operatorname* { m a x } } = 1 / 2 < 1 / \sqrt { 3 }$ . The proposition concerns arbitrary 0-1 matrices only; the bandmembership matrices of Section 5 have additional structure (two bands, one of them extremely thin), and whether that structure admits a rounding statement with a larger tolerance is open.